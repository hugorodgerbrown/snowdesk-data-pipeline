{
  "name": "snowdesk-data-pipeline",
  "scripts": {
    "lh": "DJANGO_SETTINGS_MODULE=config.settings.perf uv run python manage.py collectstatic --noinput && lhci autorun",
    "lh:open": "open $(python -c \"import json; print(' '.join(r['htmlPath'] for r in json.load(open('.lighthouseci/manifest.json')) if r['isRepresentativeRun']))\")",
    "build:icons": "node bin/build-pwa-icons",
    "build:screenshots": "node bin/build-pwa-screenshots",
    "test:js": "vitest run"
  },
  "devDependencies": {
    "@lhci/cli": "^0.14.0",
    "@tailwindcss/cli": "^4.2.2",
    "fake-indexeddb": "^6.0.0",
    "jsdom": "^25.0.1",
    "sharp": "^0.34.0",
    "vitest": "^3.0.0"
  },
  "comments": {
    "overrides": "Force patched versions of vulnerable transitive deps of @lhci/cli (dev-only Lighthouse CI; never shipped to a browser or run in production). The latest @lhci/cli still pins these, and `npm audit fix --force` only offers a breaking downgrade to @lhci/cli@0.1.0 — do not run it. See SNOW-440 / docs/lighthouse.md."
  },
  "overrides": {
    "tmp": "^0.2.6",
    "uuid": "^11.1.1",
    "cookie": "^0.7.0"
  }
}
