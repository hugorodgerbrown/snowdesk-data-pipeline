# Code review — 2026-06-03

**Reviewer:** Hugo Rodger-Brown
**Branch:** feature/snow-280
**Tox baseline:** all green (2698 passed, 7 skipped, 93% coverage)
**Previous cycle:** [2026-05-20](2026-05-20.md) (SNOW-210)

## Summary

Codebase is in good health heading into cycle 4. The recent SNOW-275–SNOW-279
commits (PostHog middleware migration, favicon redirect, RequestLog request-context
capture, MaxMind fetcher hardening) all follow project conventions — module
headers, docstrings, HTMX guards, and tz-awareness are all correct. The main
finding is four `logger.warning/error(..., exc_info=True)` calls inside `except`
blocks across `analytics/__init__.py`, `bulletins/services/geoip.py`, and
`bulletins/models.py`; these were fixed inline with `logger.exception` as the
prior cycle established. Carry-forward watching items are unchanged. Two
coverage gaps below the 90% threshold (`public/views.py` at 82%,
`public/templatetags/og_tags.py` at 83%) are spun off. The `dev_magic_link`
command at 0% coverage is added as a new spin-off. SNOW-213 (test `-> None`
annotations) and SNOW-214 (`_parse_date` consolidation) remain open upstream
tickets; their status is noted under watching.

## Inline-fixed

| Item | File(s) | Why trivial |
|------|---------|-------------|
| `logger.warning/error` + `exc_info=True` inside `except` blocks → `logger.exception`; drop redundant `exc_info=True` | `analytics/__init__.py` (2 sites: `track()` and `alias()`), `bulletins/services/geoip.py` (`_get_reader()`), `bulletins/models.py` (`PipelineRun.mark_failed()`) | Mechanical substitution; no behaviour change; consistent with cycle 3 fix pattern |
| Drop duplicate `logger.exception` call when `PipelineRun.mark_failed` already logs the same exception | `bulletins/services/meteofrance_archive_loader.py` | Same exception was being logged twice once `mark_failed` started using `logger.exception` in this cycle; surfaced by reviewer. Kept fallback log for the `run is None` (non-commit) branch so traceback isn't silently swallowed |

## Spun off

| Item | Child ticket | Why non-trivial |
|------|--------------|-----------------|
| `public/views.py` coverage 82% → ≥ 90% | [SNOW-281](https://linear.app/hugorodgerbrown/issue/SNOW-281) | 886 statements, 159 missing; large file requiring many new test cases across share, JSON-LD, weather snippet, season calendar, and error paths |
| `public/templatetags/og_tags.py` coverage 83% → ≥ 90% | [SNOW-282](https://linear.app/hugorodgerbrown/issue/SNOW-282) | 18 statements, 3 missing; the uncovered path is the `except (AttributeError, IndexError)` branch in `og_locale` — needs a test for malformed input that triggers that exception |
| `subscriptions/management/commands/dev_magic_link.py` coverage 0% → ≥ 90% | [SNOW-283](https://linear.app/hugorodgerbrown/issue/SNOW-283) | 31 statements, 31 missing; dev-only command with a `DEBUG=False` guard; needs tests that monkeypatch `settings.DEBUG = True` and exercise the create / activate / token paths |

## Watching

| Item | Notes |
|------|-------|
| `RegionBulletin` admin coverage | Carry from SNOW-165 / SNOW-210. `RegionBulletinInline` under `BulletinAdmin`, no top-level admin. Inline arguably satisfies the "explicit AdminModel" rule. Deferred again; re-check next cycle |
| `Subscription`/`PasskeyCredential` not inheriting `BaseModel` | Carry from SNOW-165 / SNOW-210. Both models inline the standard fields without `BaseModel` — intentional due to `AbstractBaseUser` constraint on `Subscriber`. No code comment explaining the exception; adding one would clarify intent. Left watching |
| `print()` in `docs/research/meteofrance/_parse_samples.py` | Research script; acceptable. No action |
| `config/settings/base.py` imports `logging` without a module-level logger | Settings file; module-level logger unused by convention. Acceptable. No action |
| SNOW-213 — test `-> None` annotations | 402 of 2458 (16%) test functions still lack `-> None`; work ongoing upstream. Re-check next cycle |
| SNOW-214 — `_parse_date` consolidation | No `bulletins/management/commands/_utils.py` yet; the `_parse_date` pattern is no longer duplicated across the commands checked, but SNOW-214 remains open. Re-check next cycle |

## Checklist results

1. Module-header docstrings — ✅ no drift found (all non-`__init__` modules in `analytics/`, `core/`, `bulletins/`, `public/`, `subscriptions/`, `regions/` have headers; SNOW-275–279 additions fully compliant)
2. Function/class docstrings — ✅ no drift found (spot-checked 20+ functions across `analytics/__init__.py`, `core/http.py`, `core/locale.py`, `core/services/request_log.py`, `core/models.py`, `bulletins/services/geoip.py`; all documented)
3. Logging discipline — 🔧 fixed inline (4 `logger.warning/error` + `exc_info=True` in `except` blocks → `logger.exception` across 3 files; 2 intentional `logger.warning` in `subscriptions/views.py` for `DoesNotExist` control-flow paths left as-is per cycle 3 decision)
4. Type annotations (test side) — 👀 watching (402/2458 test functions still lack `-> None`; SNOW-213 ongoing)
5. Datetime tz-awareness — ✅ no drift found (no `datetime.now()` or `datetime.utcnow()` in production source; factory datetimes carry `tzinfo=UTC`; `RequestLog` model uses `auto_now_add`)
6. Model rules — ✅ no new drift (`RequestLog` from SNOW-277 has `BaseModel`, custom `QuerySet`, `Manager`, `AdminModel`, factory, test module, `to_string()`, `Meta.ordering`; all 9 concrete models + `RequestLog` compliant; `RegionBulletin`/`Subscription`/`PasskeyCredential` exceptions unchanged)
7. No business logic in models — ✅ no drift found
8. No `post_save` signals — ✅ no drift found
9. HTMX partial views — ✅ no drift found (`@require_htmx` present on all 5 `partials/` routes across `public/views.py` and `public/debug_views.py`; SNOW-277 `subscriptions` views guarded correctly)
10. No DB lookups in templates/templatetags — ✅ no drift found
11. Management-command shape — ✅ 17/17 commands compliant (`reformat_mf_comments` added since cycle 3 follows `--commit` pattern; sensible defaults; `--verbosity` respected; non-zero exit on failure)
12. Project invariants — ✅ all clean (`mark_safe` only on codebase-controlled strings; email lowercase invariant now in forms, queryset manager, and all view paths — SNOW-212 closed; async email via `@task`; HTMX guards hold; no secrets in source)
13. Dead code — ✅ no drift found (no dead code from SNOW-275–279; PostHog migration left no orphaned custom middleware; favicon redirect is wired and tested)
14. Unused dependencies — ✅ no drift found (`posthog` added in SNOW-275/266 is present in `test`, `django-checks`, and `mypy` tox envs; all other runtime deps cross-checked against tox `deps` blocks)
15. Pattern consistency — 👀 watching (no new duplication introduced; SNOW-214 `_parse_date` consolidation still pending)
16. Test coverage — 📋 spun off (93% overall above threshold; per-module gaps: [SNOW-281](https://linear.app/hugorodgerbrown/issue/SNOW-281) `public/views.py` 82%, [SNOW-282](https://linear.app/hugorodgerbrown/issue/SNOW-282) `public/templatetags/og_tags.py` 83%, [SNOW-283](https://linear.app/hugorodgerbrown/issue/SNOW-283) `subscriptions/management/commands/dev_magic_link.py` 0%; `mint_vapid_keypair.py` 86% and `dump_resorts_fixture.py` 89% are borderline but not yet actioned)
17. Stale TODO/FIXME/XXX/HACK — ✅ no drift found (one `\uXXXX` in `dump_resorts_fixture.py` docstring is a literal text example, not a marker)
