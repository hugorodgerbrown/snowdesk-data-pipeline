# Code review — 2026-07-26

**Reviewer:** Code review Routine
**Branch:** claude/charming-lovelace-5y6aqy
**Tox baseline:** all green (4822 passed, 8 skipped, 95% coverage)
**Previous cycle:** [2026-07-19](2026-07-19.md) (SNOW-450)

## Summary

A much larger cycle than 2026-07-19: 49 commits, 266 files changed, +35,334/-8,911 lines, dominated by the offline-map/PWA basemap-download work (SNOW-521, SNOW-524, SNOW-518, SNOW-517), a resort-page feature arc (SNOW-499/500/501/503/504/508/509), and — most significantly — SNOW-514, which collapsed the `Subscriber` model into `Account`. Coverage rose 94% → 95%: `accounts/admin.py` crossed the 90% threshold (89% → 92%, resolved), and the persistent `seed_dev_users.py` 73% coverage gap (SNOW-349) disappeared entirely because SNOW-454 folded that command into `seed_test_data.py` (now 94% covered) — SNOW-349 has been cancelled as a result. Conventions held well elsewhere: zero `ruff`/`ds-lint`/`mypy` violations, no `post_save` signals, no DB lookups in templatetags, every HTMX partial correctly guarded, and all project invariants intact.

The seven tickets reopened last cycle after the PR-body auto-close bug (SNOW-166, SNOW-363, SNOW-364, SNOW-365, SNOW-366, SNOW-337, SNOW-321) were independently re-verified this cycle both in Linear (all still `Backlog`, i.e. genuinely open — no further false closures since 2026-07-19) and in the code (all seven findings unchanged, byte-for-byte, from last cycle's metrics). The underlying bug, SNOW-451, is still unfixed in Backlog, so this cycle's PR again omits the child-ticket list from its body — see "Process note" below.

Two genuinely new findings this cycle. First, the `Subscription`/`PasskeyCredential`/`PushSubscription` BaseModel-exception watch item — carried unchanged since 2026-05-05 with the rationale "constrained by `AbstractBaseUser`/spike design" — turns out to rest on a stale premise: none of the three ever extended `AbstractBaseUser`. The model that plausibly did (the old `Subscriber`) is exactly the one SNOW-514 collapsed into `Account`, which now correctly inherits `BaseModel`. Escalated from watching to a spin-off (SNOW-530) so someone actually re-checks whether the exception still holds. Second, `core/migrations/0006_requestlog_account.py` (new via SNOW-514) sits at 70% coverage, and is the only app's migrations directory not present in `pyproject.toml`'s coverage `omit` list — spun off as SNOW-531 with two acceptable fixes (test the backfill branch, or extend the omit list for consistency).

One inline fix landed: a stale `TODO(SNOW-848)` placeholder ticket reference in `static/js/db.js` (no real ticket exists for that follow-up) was reworded to a plain non-ticket note rather than left pointing at a fake identifier.

## Inline-fixed

| Item | File(s) | Why trivial |
|------|---------|-------------|
| `TODO(SNOW-848)` placeholder ticket reference reworded to a plain note (no real ticket exists for the eviction-heuristic follow-up) | `static/js/db.js` | Single-file comment wording change, no behaviour change |

## Spun off

| Item | Child ticket | Why non-trivial |
|------|--------------|-----------------|
| Re-examine `BaseModel` exception for `Subscription`/`PasskeyCredential`/`PushSubscription` — stale rationale post-SNOW-514 | [SNOW-530](https://linear.app/hugorodgerbrown/issue/SNOW-530) | Touches three model definitions plus migrations; PK/field changes need careful FK/admin/test review |
| `core/migrations/0006_requestlog_account.py` 70% coverage / `core/migrations` missing from coverage omit-list | [SNOW-531](https://linear.app/hugorodgerbrown/issue/SNOW-531) | Needs either a new migration-data test or a `pyproject.toml` change with documented rationale |

## Watching

| Item | Notes |
|------|-------|
| `recompute_day_ratings.py` 0% coverage | Tracked by [SNOW-166](https://linear.app/hugorodgerbrown/issue/SNOW-166) (Backlog, open — confirmed genuinely unresolved this cycle, not falsely closed again). Carry |
| `bulletins/services/geoip.py` 74% coverage | Tracked by [SNOW-363](https://linear.app/hugorodgerbrown/issue/SNOW-363) (Backlog, open). Carry |
| `bulletins/services/meteofrance_fetcher.py` 88% coverage | Tracked by [SNOW-364](https://linear.app/hugorodgerbrown/issue/SNOW-364) (Backlog, open). Carry |
| `bulletins/services/meteofrance_translator.py` 87% coverage | Tracked by [SNOW-365](https://linear.app/hugorodgerbrown/issue/SNOW-365) (Backlog, open). Carry |
| `backfill_bulletin_groupings.py` 89% coverage | Tracked by [SNOW-366](https://linear.app/hugorodgerbrown/issue/SNOW-366) (Backlog, open). Carry |
| `core/locale.py` 78% coverage | Tracked by [SNOW-337](https://linear.app/hugorodgerbrown/issue/SNOW-337) (Backlog, open). Carry |
| `py_vapid` missing from `pyproject.toml` runtime deps | Tracked by [SNOW-321](https://linear.app/hugorodgerbrown/issue/SNOW-321) (Backlog, open); still directly imported in `accounts/management/commands/mint_vapid_keypair.py` with no direct declaration. Carry |
| `analytics/schema.py` 89% coverage | Tracked by [SNOW-453](https://linear.app/hugorodgerbrown/issue/SNOW-453) (Backlog, open, unchanged). Carry |
| `_parse_latlon`/`_parse_gps` duplication (`favourites/views.py`, `observations/views.py`) | Tracked by [SNOW-452](https://linear.app/hugorodgerbrown/issue/SNOW-452) (Backlog, open, unchanged). Carry |
| `PipelineRun`/`Bulletin`/`RegionBulletin` missing `to_string()` delegator | Tracked by [SNOW-350](https://linear.app/hugorodgerbrown/issue/SNOW-350) (Backlog, open, unchanged). Carry |
| `_get_properties` pattern duplication | `public/views.py`, `bulletins/admin.py:279`, `bulletins/management/commands/parse_wet_snow_coverage.py:296`. Below extraction threshold. Carry |
| `MicroRegionNeighbour` through-table exception | Plain `models.Model`, no `BaseModel`/`to_string()`/admin/factory — accepted through-table exception since 2026-05-05. Carry |
| `public/views.py` coverage gap | 86% (1234 stmts, 167 missing) — improved from 85% despite +41 net statements. Carry |
| `public/templatetags/og_tags.py` coverage gap | 83% (18 stmts, 3 missing); uncovered `except (AttributeError, IndexError)` branch in `og_locale`. Carry |
| `mint_vapid_keypair.py` 86%, `core/apps.py` 86%, `dump_resorts_fixture.py` 89% | All below 90% in narrow error-handling branches; gaps stable. Carry |
| `seed_test_data.py` requires one of `--all`/`--include`/`--exclude` | Bare invocation errors rather than defaulting — documented as a deliberate guard (SNOW-444) against an ambiguous "seed everything" default. Unchanged this cycle. Carry |
| `psycopg[binary]` absent from `tox.ini` `deps =` blocks | Confirmed intentional again this cycle — tox envs run against SQLite; the dependency is only exercised in production. Not a drift item |
| `regions/services/point_match.py:149` `logger.debug(..., exc_info=True)` | Confirmed correct debug-level form. Carry |
| `print()` in `docs/research/meteofrance/_parse_samples.py` | Research script; acceptable. Carry |
| `config/settings/base.py` imports `logging` without a module-level logger | Settings convention; acceptable. Carry |
| SNOW-451 (PR-body auto-close bug) still unfixed | Confirmed Backlog. This cycle's PR again omits the "Child tickets" list from its body as mitigation. Carry until SNOW-451 lands |

## Checklist results

1. Module-header docstrings — ✅ no drift found (AST-walked every non-test, non-migration `.py` across all apps; the handful of files without a formal docstring are bare/near-bare `__init__.py` package markers, stable since every prior cycle)
2. Function/class docstrings — ✅ no drift found (spot-checked 264 functions/classes across the 10 most-touched modules this cycle, including `basemap_tiles.py`, `sw_shell.py`, `waffle_sync.py` — zero missing)
3. Logging discipline — ✅ no drift found (no `print()` outside the known research-script exception; `regions/services/point_match.py:149` `exc_info=True` confirmed correct debug-level form)
4. Type annotations (test side) — ✅ no drift found (`tox -e mypy` runs `disallow_untyped_defs` over `tests/` too, 516 source files, 0 issues)
5. Datetime tz-awareness — ✅ no drift found (all production `datetime(...)` calls carry explicit `tzinfo`/`ZoneInfo`; no naive `datetime.now()`/`utcnow()` in production source)
6. Model rules — 📋 spun off (new/escalated this cycle: `Subscription`/`PasskeyCredential`/`PushSubscription` BaseModel-exception rationale looks stale post-SNOW-514 — SNOW-530; `PipelineRun`/`Bulletin`/`RegionBulletin` missing `to_string()` — SNOW-350 unchanged; all new concrete models this cycle confirmed to ship the full kit; `MicroRegionNeighbour` through-table exception carries as watching)
7. No business logic in models — ✅ no drift found
8. No `post_save` signals — ✅ no drift found (only hit is the explanatory comment at `bulletins/services/day_rating.py:74`)
9. HTMX partial views — ✅ no drift found (every `partials/`-prefixed route across `public`, `favourites`, `observations` correctly `@require_htmx`-guarded; documented non-`partials/` exceptions remain correctly decorated per their app's convention)
10. No DB lookups in templates/templatetags — ✅ no drift found (zero `.objects.` matches across all `templatetags/*.py`)
11. Management-command shape — 👀 watching (two new commands this cycle, `compute_basemap_download` and `link_resort_forecast_points`, both fully compliant; `seed_test_data`'s required-selection-flag exception carries unchanged as documented/deliberate — SNOW-444)
12. Project invariants — ✅ all clean (`mark_safe` sites carry sanitisation justification; email lowercasing consistent; all `send_mail()` calls inside `@task()` workers; every partial route `@require_htmx`; no secrets in source)
13. Dead code — ✅ no drift found (`tox -e lint` clean; `ds-lint` 0 violations across 114 templates; no commented-out code blocks)
14. Unused dependencies — 👀 watching (`py_vapid` still missing from `pyproject.toml` runtime deps — SNOW-321, confirmed still open; `psycopg[binary]` absence from `tox.ini` confirmed intentional; all other runtime deps confirmed used)
15. Pattern consistency — 📋 spun off (new this cycle: `core/migrations` omit-list inconsistency in `pyproject.toml` — SNOW-531; `_parse_latlon`/`_parse_gps` duplication confirmed still present — SNOW-452 unchanged; `_get_properties` triplication carries as watching)
16. Test coverage — 📋 spun off (4822 passed, 8 skipped, 95% overall — up from 94%. Resolved this cycle: `accounts/admin.py` 89%→92%; `seed_dev_users.py` 73% gap eliminated by SNOW-454 folding it into `seed_test_data.py`, now 94% [SNOW-349 cancelled]. New-to-tracking gap: `core/migrations/0006_requestlog_account.py` 70% [SNOW-531]. Unchanged carry: `recompute_day_ratings.py` 0% [SNOW-166], `geoip.py` 74% [SNOW-363], `meteofrance_fetcher.py` 88% [SNOW-364], `meteofrance_translator.py` 87% [SNOW-365], `backfill_bulletin_groupings.py` 89% [SNOW-366], `core/locale.py` 78% [SNOW-337], `analytics/schema.py` 89% [SNOW-453], `public/views.py` 86%, `og_tags.py` 83%, several 86–89% modules stable)
17. Stale TODO/FIXME/XXX/HACK — 🔧 fixed inline (`static/js/db.js:158` `TODO(SNOW-848)` placeholder ticket number reworded to a plain note; all other raw regex hits — `CH-XXXX` domain placeholders, `Vent XXXX m` regex-comment placeholder, `\uXXXX` escape-sequence docstring example — confirmed literal-text false positives, unchanged from every prior cycle)

## Process note: ticket-closure integrity (carried from 2026-07-19)

SNOW-451, tracking the fix for the PR-body auto-close bug that silently marked seven follow-up tickets Done without any corresponding fix landing (see the 2026-07-19 doc for the full mechanism writeup), is still unfixed and sitting in Backlog. This cycle independently re-verified via Linear that all seven previously-reopened tickets (SNOW-166, SNOW-363, SNOW-364, SNOW-365, SNOW-366, SNOW-337, SNOW-321) remain genuinely open — no further false closures occurred since the 2026-07-19 reopening, because no PR body has referenced them by identifier since. This cycle's PR continues the same mitigation: the "Child tickets" list is omitted from the PR body, with all child-ticket links living in this doc instead.
