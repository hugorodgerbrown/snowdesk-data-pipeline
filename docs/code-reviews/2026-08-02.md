# Code review — 2026-08-02

**Reviewer:** Code review Routine
**Branch:** claude/charming-lovelace-go8345
**Tox baseline:** all green (5272 passed, 8 skipped, 95% coverage)
**Previous cycle:** [2026-07-26](2026-07-26.md) (SNOW-529)

## Summary

A large, healthy cycle: 45 commits since 2026-07-26, dominated by the offline-map basemap-download arc (SNOW-566/567/568/569/570/581/583/586/587), Open-Meteo host/key configurability (SNOW-577/579), point-forecast convergence history (SNOW-575, new `ForecastPointWeatherHistory` model), Météo-France re-identity work (SNOW-559/562/581), the `apps/` project-structure move (SNOW-557), health-check endpoints (SNOW-565), and the golden-week seeding feature (SNOW-528). Test count grew by 450 (4822 → 5272) while overall coverage held flat at 95%. Conventions held well: `ruff`, `mypy`, `ds-lint`, and `django-checks` are all clean, no `post_save` signals, no DB lookups in templatetags, every HTMX partial correctly guarded, and all five project invariants intact. The three new management commands this cycle (`backfill_bulletin_source`, `rekey_meteofrance_bulletins`, `purge_legacy_meteofrance_bulletins`) ship fully compliant with the four-rule contract, and the one new concrete model (`ForecastPointWeatherHistory`, SNOW-575) ships the complete model kit.

One genuine partial resolution: `Bulletin` picked up a proper `to_string()`/`__str__` delegator as a side effect of SNOW-560, narrowing the SNOW-350 gap to `PipelineRun` and `RegionBulletin`.

Three genuinely new findings this cycle. First, three CSS rules in `src/css/main.css` — `.hero-rating-badge`, `.field-guidance-trigger`, `.danger-tile--inline` — had zero references anywhere in the template tree, JS, or Python; removed inline as dead code (checklist #13). Pulling that thread further turned up something the surface grep didn't catch: the *backend* logic that used to feed the hero badge and its companion period-transition chip — `_build_morning_rating()`/`_build_period_transition_chip()` in `apps/public/views.py`, plus matching fixture variants in `apps/public/_component_fixtures.py` — is itself orphaned. Neither `morning_rating` nor `period_transition_chip` is referenced by name in any template; the header panel was rebuilt around `_weather_panel.html` during SNOW-509 without the old computation being removed, and the two tickets that plausibly should have caught this (SNOW-246, SNOW-248, both Done; SNOW-488 "Prune redundant compatibility and transition code paths", also Done) didn't. That's multi-file and needs a design call (delete vs. re-wire), so it's spun off as SNOW-595 rather than inline-fixed. Second and third, two modules newly dipped under the 90% coverage line: the brand-new `apps/bulletins/services/golden_week.py` (86%, SNOW-528) and `apps/core/models.py` (87%, previously ≥90%, concentrated in the `IdempotencyRecord` race-retry branch) — spun off as SNOW-593 and SNOW-594.

`py_vapid` (SNOW-321) remains undeclared as a direct runtime dependency; `core/migrations` remains absent from the coverage omit-list (SNOW-531); the `Subscription`/`PasskeyCredential`/`PushSubscription` `BaseModel` exception rationale remains unaddressed (SNOW-530). All three carry unchanged. Linear access confirms the seven previously-reopened tickets from the SNOW-451 PR-body auto-close bug (SNOW-166, SNOW-363, SNOW-364, SNOW-365, SNOW-366, SNOW-337, SNOW-321) are still genuinely open in Backlog, and no commit in this cycle's 45-commit range references any of those identifiers by ID — so no further false-closure risk arose. SNOW-451 itself is still unfixed, so this cycle's PR again omits the child-ticket list from its body as mitigation.

## Inline-fixed

| Item | File(s) | Why trivial |
|------|---------|-------------|
| Removed three dead CSS rules with zero template/JS references (`.hero-rating-badge` incl. all `data-level` variants, `.field-guidance-trigger` incl. pseudo-element rules, `.danger-tile--inline`) | `src/css/main.css` | Single-file, no behaviour change (rules were unreferenced), no new tests or abstractions |

## Spun off

| Item | Child ticket | Why non-trivial |
|------|--------------|-----------------|
| Remove orphaned `_build_morning_rating`/`_build_period_transition_chip` backend logic and matching design-system fixtures left over from the SNOW-509 header redesign | [SNOW-595](https://linear.app/hugorodgerbrown/issue/SNOW-595) | Spans `views.py` + `_component_fixtures.py` plus test coverage; needs a design call (delete vs. re-wire) and a check that nothing else depends on the fixture variants |
| `apps/bulletins/services/golden_week.py` 86% coverage (new module, SNOW-528) | [SNOW-593](https://linear.app/hugorodgerbrown/issue/SNOW-593) | Needs new tests covering error/fallback branches in the golden-week derivation logic |
| `apps/core/models.py` 87% coverage (newly dropped from ≥90%) | [SNOW-594](https://linear.app/hugorodgerbrown/issue/SNOW-594) | Gap is in the `IdempotencyRecord` race-retry branch; needs a test that provokes the race, not a one-liner |

## Watching

| Item | Notes |
|------|-------|
| `recompute_day_ratings.py` 0% coverage | Tracked by [SNOW-166](https://linear.app/hugorodgerbrown/issue/SNOW-166) (Backlog, open). Carry |
| `bulletins/services/geoip.py` 74% coverage | Tracked by [SNOW-363](https://linear.app/hugorodgerbrown/issue/SNOW-363) (Backlog, open). Carry |
| `bulletins/services/meteofrance_fetcher.py` 88% coverage | Tracked by [SNOW-364](https://linear.app/hugorodgerbrown/issue/SNOW-364) (Backlog, open). Carry |
| `bulletins/services/meteofrance_translator.py` 87% coverage | Tracked by [SNOW-365](https://linear.app/hugorodgerbrown/issue/SNOW-365) (Backlog, open). Carry |
| `backfill_bulletin_groupings.py` 89% coverage | Tracked by [SNOW-366](https://linear.app/hugorodgerbrown/issue/SNOW-366) (Backlog, open). Carry |
| `core/locale.py` 78% coverage | Tracked by [SNOW-337](https://linear.app/hugorodgerbrown/issue/SNOW-337) (Backlog, open). Carry |
| `py_vapid` missing from `pyproject.toml` runtime deps | Tracked by [SNOW-321](https://linear.app/hugorodgerbrown/issue/SNOW-321) (Backlog, open); still directly imported in `accounts/management/commands/mint_vapid_keypair.py` with no direct declaration. Carry |
| `analytics/schema.py` 89% coverage | Tracked by [SNOW-453](https://linear.app/hugorodgerbrown/issue/SNOW-453) (Backlog, open). Carry |
| `_parse_latlon`/`_parse_gps` duplication (`favourites/views.py`, `observations/views.py`) | Tracked by [SNOW-452](https://linear.app/hugorodgerbrown/issue/SNOW-452) (Backlog, open). Carry |
| `PipelineRun`/`RegionBulletin` missing `to_string()` delegator | Tracked by [SNOW-350](https://linear.app/hugorodgerbrown/issue/SNOW-350) (Backlog, open) — scope narrowed this cycle: `Bulletin` gained `to_string()` as a SNOW-560 side effect, so only two models remain. Carry |
| `_get_properties` pattern duplication | `public/views.py`, `bulletins/admin.py:295`, `bulletins/management/commands/parse_wet_snow_coverage.py:296`. Below extraction threshold. Carry |
| `Subscription`/`PasskeyCredential`/`PushSubscription` `BaseModel` exception | Tracked by [SNOW-530](https://linear.app/hugorodgerbrown/issue/SNOW-530) (Backlog, open, unaddressed). Carry |
| `MicroRegionNeighbour` through-table exception | Plain `models.Model`, no `BaseModel`/`to_string()`/admin/factory — accepted through-table exception since 2026-05-05. Carry |
| `core/migrations/0006_requestlog_account.py` 70% coverage / `core/migrations` missing from coverage omit-list | Tracked by [SNOW-531](https://linear.app/hugorodgerbrown/issue/SNOW-531) (Backlog, open, unaddressed). Carry |
| `public/views.py` coverage gap | 88% (1257 stmts, 151 missing) — improved from 86% despite +23 net statements. Carry |
| `public/templatetags/og_tags.py` coverage gap | 83% (23 stmts, 4 missing); unchanged. Carry |
| `mint_vapid_keypair.py` 86%, `dump_resorts_fixture.py` 89%, `core/apps.py` 89% (improved from 86%) | All below 90% in narrow error-handling branches; gaps stable or improving. Carry |
| `seed_test_data.py` requires one of `--all`/`--include`/`--exclude` | Deliberate guard (SNOW-444) against an ambiguous "seed everything" default. Unchanged. Carry |
| `psycopg[binary]` absent from `tox.ini` `deps =` blocks | Confirmed intentional again this cycle — tox envs run against SQLite. Not a drift item |
| `regions/services/point_match.py:149` `logger.debug(..., exc_info=True)` | Confirmed correct debug-level form. Carry |
| `print()` in `docs/research/meteofrance/_parse_samples.py` | Research script; acceptable. Carry |
| `config/settings/base.py` imports `logging` without a module-level logger | Settings convention; acceptable. Carry |
| SNOW-451 (PR-body auto-close bug) still unfixed | Confirmed Backlog. This cycle's PR again omits the "Child tickets" list from its body as mitigation. Carry until SNOW-451 lands |

## Checklist results

1. Module-header docstrings — ✅ no drift found (AST-walked every non-test, non-migration `.py` under `apps/` and `config/`, 191 files; the 21 without a docstring are all bare `__init__.py` package markers, unchanged in kind from every prior cycle)
2. Function/class docstrings — ✅ no drift found (AST-checked all 1444 functions/classes across the 182 files touched this cycle plus a 25-item random spot-check elsewhere; the sole miss, a private nested closure in `bulletins/services/coverage.py`, is fully documented by its enclosing function)
3. Logging discipline — ✅ no drift found (no `print()` outside the known research-script exception; the one `exc_info=True` hit, `regions/services/point_match.py:149`, confirmed correct debug-level form)
4. Type annotations (test side) — ✅ no drift found (`tox -e mypy` runs `disallow_untyped_defs` over `tests/` too, 566 source files, 0 issues; the sole return-annotation gap is an explicitly `# type: ignore[no-untyped-def]`-marked mock closure)
5. Datetime tz-awareness — ✅ no drift found (no naive `datetime.now()`/`utcnow()` in production source; all `datetime(...)` construction carries explicit `tzinfo`/`ZoneInfo`)
6. Model rules — 👀 watching (new `ForecastPointWeatherHistory` model, SNOW-575, ships the full kit; `Bulletin` gained `to_string()` as a SNOW-560 side effect, narrowing SNOW-350 to `PipelineRun`/`RegionBulletin`; `Subscription`/`PasskeyCredential`/`PushSubscription` and `MicroRegionNeighbour` exceptions unchanged — SNOW-530 still open)
7. No business logic in models — ✅ no drift found
8. No `post_save` signals — ✅ no drift found (only hit is the explanatory comment at `bulletins/services/day_rating.py:75`)
9. HTMX partial views — ✅ no drift found (every `partials/`-prefixed route across `public`, `favourites`, `observations` correctly `@require_htmx`-guarded)
10. No DB lookups in templates/templatetags — ✅ no drift found (zero ORM matches across all `templatetags/*.py`)
11. Management-command shape — ✅ no drift found (three new commands this cycle — `backfill_bulletin_source`, `rekey_meteofrance_bulletins`, `purge_legacy_meteofrance_bulletins` — all fully compliant; `seed_test_data`'s required-selection-flag exception carries unchanged — SNOW-444)
12. Project invariants — ✅ all clean (`mark_safe` sites carry sanitisation justification; email lowercasing consistent; all `send_mail()` calls inside `@task()` workers; every partial route `@require_htmx`; no secrets in source)
13. Dead code — 🔧 fixed inline / 📋 spun off (removed three dead CSS rules with zero references — `.hero-rating-badge`, `.field-guidance-trigger`, `.danger-tile--inline`; the backend logic that fed them turned out to be independently orphaned too, spun off as SNOW-595 since it spans two Python modules and needs a design call)
14. Unused dependencies — 👀 watching (`py_vapid` still missing from `pyproject.toml` runtime deps — SNOW-321; `psycopg[binary]` absence from `tox.ini` confirmed intentional; all other runtime deps confirmed used)
15. Pattern consistency — 📋 spun off (orphaned hero-badge/period-transition-chip backend logic — SNOW-595, new this cycle; `_parse_latlon`/`_parse_gps` duplication confirmed still present — SNOW-452 unchanged; `core/migrations` omit-list inconsistency — SNOW-531 unchanged; `_get_properties` triplication carries as watching)
16. Test coverage — 📋 spun off (5272 passed, 8 skipped, 95% overall — up from 4822 passed at the same 95%. New gaps below 90%: `golden_week.py` 86% [new module, SNOW-528] spun off as SNOW-593; `core/models.py` 87% [newly dropped from ≥90%] spun off as SNOW-594. Unchanged carry: `recompute_day_ratings.py` 0% [SNOW-166], `geoip.py` 74% [SNOW-363], `meteofrance_fetcher.py` 88% [SNOW-364], `meteofrance_translator.py` 87% [SNOW-365], `backfill_bulletin_groupings.py` 89% [SNOW-366], `core/locale.py` 78% [SNOW-337], `core/migrations/0006_requestlog_account.py` 70% [SNOW-531], `analytics/schema.py` 89% [SNOW-453], `mint_vapid_keypair.py` 86%, `dump_resorts_fixture.py` 89%, `core/apps.py` 89% [improved from 86%], `public/views.py` 88% [improved from 86%], `og_tags.py` 83%)
17. Stale TODO/FIXME/XXX/HACK — ✅ no drift found (only non-marker hits are literal-text false positives — `CH-XXXX` domain placeholders, `Vent XXXX m` regex-comment placeholder, `\uXXXX` escape-sequence docstring example, and last cycle's plain non-ticket `TODO:` note in `static/js/db.js`, confirmed unchanged and still ticket-free as intended)

## Process note: ticket-closure integrity (carried from 2026-07-19 / 2026-07-26)

SNOW-451, tracking the fix for the PR-body auto-close bug that silently marked seven follow-up tickets Done without any corresponding fix landing, is still unfixed and sitting in Backlog. This cycle re-verified via Linear that all seven previously-reopened tickets (SNOW-166, SNOW-363, SNOW-364, SNOW-365, SNOW-366, SNOW-337, SNOW-321) remain genuinely open — no further false closures occurred since the 2026-07-26 reopening-confirmation, because no PR body referenced them by identifier since. This cycle's PR continues the same mitigation: the "Child tickets" list is omitted from the PR body, with all child-ticket links living in this doc instead.
