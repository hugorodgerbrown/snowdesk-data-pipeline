# Code review — 2026-06-21

**Reviewer:** Code review Routine
**Branch:** claude/charming-lovelace-47jsn6
**Tox baseline:** all green (3581 passed, 7 skipped, 93% coverage)
**Previous cycle:** [2026-06-15](2026-06-15.md) (SNOW-320)

## Summary

Cycle 7 covers 14 commits since 2026-06-15: the most significant are the Poetry → uv migration (SNOW-325) and the new `observations` app for GPS-gated field reports (SNOW-324/330/333). Codebase health is good overall — invariants hold, module headers and docstrings are complete across all new files, no `print()` or `post_save` drift, and `FieldObservation` (new this cycle) is fully model-kit-compliant. Two new spin-offs: the `observations` app is entirely absent from the coverage configuration (SNOW-336), and `core/locale.py` dropped to 78% (SNOW-337). SNOW-213 (test `-> None` annotations) appears resolved in the non-e2e suite: 0 of 3277 non-e2e test functions now lack the annotation, down from 485/3111 (15.6%) last cycle. The `backfill_weather` mandatory-args watch item is closed — SNOW-327 replaced it with `fetch_weather` which has sensible no-arg defaults. One pattern-consistency note: 21 occurrences of `except E1, E2:` (unparenthesised) appear across 13 files. This is ruff's canonical form for multi-exception catches without `as` binding — ruff actively normalises the parenthesised form back to unparenthesised on format. The `except (E1, E2) as exc:` parenthesised form used elsewhere is required only when binding with `as`. No inconsistency; no fix needed.

## Inline-fixed

| Item | File(s) | Why trivial |
|------|---------|-------------|
| (none) | — | — |

## Spun off

| Item | Child ticket | Why non-trivial |
|------|--------------|-----------------|
| `observations` app (SNOW-324) absent from coverage measurement: not in `--cov=` flags or `[tool.coverage.run].source` in `pyproject.toml`/`tox.ini`; the 93% overall figure is understated | [SNOW-336](https://linear.app/hugorodgerbrown/issue/SNOW-336) | Requires config changes in two places; re-running tox may surface new below-90% gaps in `observations/` that need follow-up |
| `core/locale.py` 78% coverage (18 stmts, 4 missing); `except Exception` defensive branch in `primary_language` not exercised | [SNOW-337](https://linear.app/hugorodgerbrown/issue/SNOW-337) | Requires new test in `tests/core/`; multi-file (test file + subject) |

## Watching

| Item | Notes |
|------|-------|
| `public/views.py` coverage gap | Previously tracked by SNOW-281 (now closed). Coverage grew to 84% at 1108 stmts/182 missing — still below the 90% target. Carry |
| `public/templatetags/og_tags.py` coverage gap | Previously tracked by SNOW-282 (closed). 83% (18 stmts, 3 missing); uncovered path is the `except (AttributeError, IndexError)` branch in `og_locale`. Carry |
| `subscriptions/management/commands/dev_magic_link.py` 0% coverage | Previously tracked by SNOW-283 (closed). Still 0%; `DEBUG=False` guard in tests makes it a no-op in normal test runs. Carry |
| `Subscription`, `PasskeyCredential`, `PushSubscription` not inheriting `BaseModel` | All three manually inline `id`, `uuid`, `created_at`, `updated_at` — constrained by `AbstractBaseUser`/spike design. No code comment explains the exception. `FieldObservation` (new this cycle) correctly inherits `BaseModel`. Carry |
| Test `-> None` annotations | 0 of 3277 non-e2e test functions lack `-> None` — a dramatic improvement from 485/3111 (15.6%) last cycle. SNOW-213 appears resolved in the non-e2e suite; carry to verify against e2e tests separately |
| SNOW-321 — `py_vapid` direct dependency | SNOW-325 (uv migration) added `py_vapid` to `tox.ini` deps but did not promote it to `pyproject.toml`. SNOW-321 remains open. Carry |
| `core/apps.py` 86%, `subscriptions/admin.py` 87%, `subscriptions/backends.py` 88%, `subscriptions/management/commands/mint_vapid_keypair.py` 86%, `regions/management/commands/dump_resorts_fixture.py` 89% | All below 90% but each has only 2–11 missing statements in narrow error-handling branches. Not alarming individually; watch over next cycle to see if gaps grow |
| `_get_properties` pattern duplication | Duplicated at `public/views.py:176`, `bulletins/admin.py:273`, and `bulletins/management/commands/parse_wet_snow_coverage.py:296`. Below two-caller extraction threshold. Carry |
| `print()` in `docs/research/meteofrance/_parse_samples.py` | Research script; acceptable. No action. Carry |
| `config/settings/base.py` imports `logging` without a module-level logger | Settings convention; acceptable. No action. Carry |
| `backfill_weather` mandatory date args | **Closed** — SNOW-327 replaced `backfill_weather` with `fetch_weather`, which has sensible no-arg defaults. Watch closed |

## Checklist results

1. Module-header docstrings — ✅ no drift found (all new modules `observations/models.py`, `observations/views.py`, `observations/admin.py`, `observations/apps.py`, `observations/urls.py`, `bulletins/services/grouping.py`, `schedule.py`, `bulletins/management/commands/backfill_bulletin_groupings.py`, `bulletins/management/commands/fetch_weather.py`, `core/management/commands/run_scheduler.py` confirmed compliant)
2. Function/class docstrings — ✅ no drift found (spot-checked 32 functions/classes across new `observations/` and `bulletins/services/grouping.py` additions; all present)
3. Logging discipline — ✅ no new drift (`logger = logging.getLogger(__name__)` confirmed in all new production modules; no `print()` in non-research source; `logger.debug(..., exc_info=True)` at `regions/services/point_match.py:149` is correct form; research-script `print()` and settings-file-logger omission carry as watching)
4. Type annotations (test side) — 👀 watching (0/3277 non-e2e test functions lack `-> None` — dramatic improvement from 485/3111 last cycle; SNOW-213 appears resolved in non-e2e suite; carry to verify e2e separately)
5. Datetime tz-awareness — ✅ no drift found (no `datetime.now()` or `datetime.utcnow()` in production source; `observations/models.py` uses `timezone.now` correctly as the `default=` for `observed_at`)
6. Model rules — 👀 watching (`FieldObservation` new this cycle: inherits `BaseModel`, has `Meta.ordering=["-observed_at"]`, `to_string()`, `__str__`, `FieldObservationQuerySet`, `FieldObservationAdmin`, `FieldObservationFactory`, three test modules — fully compliant. `BulletinGrouping` (SNOW-323) also fully compliant. `PasskeyCredential`, `Subscription`, `PushSubscription` carry in the BaseModel-exception watching group unchanged)
7. No business logic in models — ✅ no drift found (`observations/models.py` methods are thin accessors and `Counter` aggregations; no I/O or network calls; `bulletins/models.py` spot-checked clean)
8. No `post_save` signals — ✅ no drift found (only `post_save` reference is the explanatory comment in `bulletins/services/day_rating.py` explicitly stating it is NOT used; no `Signal(` or `@receiver` found)
9. HTMX partial views — ✅ no drift found (all four `partials/` routes carry `@require_htmx`: `public/views.py:fetch_weather_snippet`, `public/views.py:season_calendar_partial`, `observations/views.py:report_form`, `observations/views.py:report_submit`; all verified)
10. No DB lookups in templates/templatetags — ✅ no drift found (no `.objects.` calls in any file under `public/templatetags/`)
11. Management-command shape — ✅ no new drift (new `fetch_weather` has `--commit`, read-only by default, exits non-zero; `backfill_bulletin_groupings` has `--commit`, raises `CommandError` on `failed > 0`; `backfill_weather` mandatory-args watch closed — replaced by `fetch_weather`)
12. Project invariants — ✅ all clean (`mark_safe` only on empty strings or `bleach`-sanitised content; email lowercase at `SubscriberQuerySet.get_or_create_for_email` and view entry points; all subscription emails async via `@task` + `.enqueue()`; all `partials/` endpoints carry `@require_htmx`; no credentials in source)
13. Dead code — ✅ no drift found (ruff checks passed; no unused imports or commented-out code blocks in new commits; no orphaned template partials)
14. Unused dependencies — 👀 watching (SNOW-321 carry: `py_vapid` imported directly in `mint_vapid_keypair.py` but absent from `pyproject.toml` direct deps; SNOW-325 added it to `tox.ini` but not `pyproject.toml`; SNOW-321 remains open)
15. Pattern consistency — ✅ no drift found (21 occurrences of `except E1, E2:` (unparenthesised) across 13 files initially flagged as inconsistent — confirmed as ruff's canonical form; ruff normalises `except (E1, E2):` to `except E1, E2:` on format-check. The parenthesised form in `meteofrance_translator.py:172/198/226` uses `as exc` binding which requires parens — different syntax, not inconsistent. `_get_properties` duplication watch carries)
16. Test coverage — 📋 spun off (3581 passed, 7 skipped, 93% overall; `observations` app absent from coverage config → SNOW-336; `core/locale.py` 78% → SNOW-337; `public/views.py` 84% / `og_tags.py` 83% / `dev_magic_link.py` 0% carry as watching; several modules 86–89% in narrow error paths — watching)
17. Stale TODO/FIXME/XXX/HACK — ✅ no drift found (no `TODO`, `FIXME`, or `HACK` markers in any `.py`, `.html`, or `.css` file; `XXXX` strings in `test_slf_fetcher.py`, `dump_resorts_fixture.py`, and `mf_bra_to_caaml.py` are domain strings, not markers)
