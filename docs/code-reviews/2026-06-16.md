# Code review — 2026-06-16

**Reviewer:** Code review Routine
**Branch:** chore/SNOW-322-code-review-2026-06-16
**Tox baseline:** not run — `SECRET_KEY` not available in remote execution environment; carrying forward 2698 passed, 7 skipped, 93% coverage from 2026-06-03
**Previous cycle:** [2026-06-15](2026-06-15.md) (SNOW-320)

## Summary

Codebase is in excellent health for cycle 7 (2026-06-16). The only commit since the 2026-06-15 baseline is the SNOW-320 code-review artefact itself (docs, README pointer, and the `bin/setup-remote-env` helper) — no feature or model changes this cycle. All 17 checklist items carry the same status as last cycle with no new drift. The one noteworthy flag: SNOW-321 (`py-vapid` direct dep) was marked Done on 2026-06-15 but `py-vapid` is still absent from `pyproject.toml` — the fix appears not to have landed; moved to watching until clarified. All other watching items carry forward unchanged.

## Inline-fixed

| Item | File(s) | Why trivial |
|------|---------|-------------|
| (none) | — | — |

## Spun off

| Item | Child ticket | Why non-trivial |
|------|--------------|-----------------|
| (none) | — | — |

## Watching

| Item | Notes |
|------|-------|
| `public/views.py` coverage gap | Tracked by [SNOW-281](https://linear.app/hugorodgerbrown/issue/SNOW-281). 82% at last measure. Carry |
| `public/templatetags/og_tags.py` coverage gap | Tracked by [SNOW-282](https://linear.app/hugorodgerbrown/issue/SNOW-282). 83% at last measure; uncovered path is the `except (AttributeError, IndexError)` branch in `og_locale`. Carry |
| `subscriptions/management/commands/dev_magic_link.py` 0% coverage | Tracked by [SNOW-283](https://linear.app/hugorodgerbrown/issue/SNOW-283). 0% throughout all cycles; `DEBUG=False` guard makes it a no-op in normal test runs. Carry |
| `Subscription`, `PasskeyCredential`, `PushSubscription` not inheriting `BaseModel` | All three manually inline `id`, `uuid`, `created_at`, `updated_at` — constrained by `AbstractBaseUser`/spike design. No code comment explains the exception. Carry |
| Test `-> None` annotations | 485 of 3111 test functions (15.6%) lack `-> None`; unchanged from cycle 6. SNOW-213 ongoing. Carry |
| `backfill_weather` mandatory date args | `--start`/`--end` are effectively required; acceptable for a backfill command where bare invocation is semantically meaningless. Carry from cycles 4–6 |
| `print()` in `docs/research/meteofrance/_parse_samples.py` | Research script; acceptable. No action. Carry |
| `config/settings/base.py` imports `logging` without a module-level logger | Settings file; module-level logger unused by convention. Acceptable. No action. Carry |
| `py-vapid` not declared as a direct runtime dependency | SNOW-321 was marked Done on 2026-06-15 but `py-vapid` is still absent from `pyproject.toml` — `mint_vapid_keypair.py:46` imports it directly; it still arrives only as a transitive dep of `pywebpush`. Fix may not have landed; flagging here until confirmed. Tracked by [SNOW-321](https://linear.app/hugorodgerbrown/issue/SNOW-321) |

## Checklist results

1. Module-header docstrings — ✅ no drift found (all non-test, non-migration, non-`__init__` modules carry a module-level docstring; SNOW-320 additions are docs-only artefacts)
2. Function/class docstrings — ✅ no drift found (spot-checked 20+ functions and classes across `core/`, `public/`, `bulletins/`, `subscriptions/`, `regions/`; all documented)
3. Logging discipline — ✅ no new drift (no `print()` in non-research source; `logger.debug(..., exc_info=True)` at `regions/services/point_match.py:148` is correct debug-level form — not a violation; research-script `print()` and settings-file logger omission are carry watching)
4. Type annotations (test side) — 👀 watching (485/3111 test functions still lack `-> None`, 15.6%; unchanged from cycle 6; SNOW-213 ongoing)
5. Datetime tz-awareness — ✅ no drift found (no `datetime.now()` or `datetime.utcnow()` in production source; all test datetimes carry `tzinfo=UTC`)
6. Model rules — 👀 watching (all 10 concrete models in `regions`, `bulletins`, and `core` fully compliant; `Subscription`, `PasskeyCredential`, `PushSubscription` remain intentional `BaseModel` exceptions — otherwise fully compliant; no change since cycle 6)
7. No business logic in models — ✅ no drift found (model methods are thin accessors, state-transition helpers, or computed properties — no I/O, fetches, or external mutations)
8. No `post_save` signals — ✅ no drift found (only `post_save` reference is a comment in `bulletins/services/day_rating.py` explicitly stating it is NOT used; no `Signal(` or `@receiver` found)
9. HTMX partial views — ✅ no drift found (all three `partials/` routes carry `@require_htmx`; `request.htmx` access only in `core/decorators.py` and test files)
10. No DB lookups in templates/templatetags — ✅ no drift found (no `.objects.` calls in any templatetag file)
11. Management-command shape — ✅ no new drift (18 commands checked; all compliant; `backfill_weather` mandatory-date watching item unchanged from cycles 4–6)
12. Project invariants — ✅ all clean (`mark_safe` only on sanitised/numeric/static content; email lowercase enforced at all entry points; subscription emails and push notifications both async via `@task` + `.enqueue()`; HTMX guards hold; no secrets in source)
13. Dead code — ✅ no drift found (no unused imports, no commented-out code blocks; no orphan partials or template tags)
14. Unused dependencies — 👀 watching (`py-vapid` imported directly at `mint_vapid_keypair.py:46` but not declared as a direct dep; SNOW-321 marked Done but fix absent from `pyproject.toml` — see watching table)
15. Pattern consistency — ✅ no drift found (SNOW-214 `_parse_date` consolidation confirmed resolved; no new helper duplication in SNOW-320 additions)
16. Test coverage — 👀 watching (tox not runnable in this environment; carrying 93% overall from 2026-06-03; SNOW-281 `public/views.py` 82%, SNOW-282 `public/templatetags/og_tags.py` 83%, SNOW-283 `dev_magic_link.py` 0% all unchanged)
17. Stale TODO/FIXME/XXX/HACK — ✅ no drift found (no `TODO`, `FIXME`, or `HACK` markers in any `.py`, `.html`, or `.css` file; `XXX`/`XXXX` strings in test and domain files are confirmed as non-markers — unchanged from prior cycles)
