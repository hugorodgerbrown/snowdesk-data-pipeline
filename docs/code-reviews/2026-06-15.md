# Code review — 2026-06-15

**Reviewer:** Code review Routine
**Branch:** claude/ecstatic-heisenberg-t6djlv
**Tox baseline:** not run — Python 3.14 not available in remote execution environment; carrying forward 2698 passed, 7 skipped, 93% coverage from 2026-06-03
**Previous cycle:** [2026-06-03](2026-06-03.md) (SNOW-280)

## Summary

Codebase is in good health for cycle 6. The five commits since the 2026-06-03 baseline — SNOW-319 (async push notifications), SNOW-318 (map popup restore), overflow fix, launch.json update, and the SNOW-269 code-review-pass skill — all follow project conventions: module headers, docstrings, HTMX guards, and tz-awareness are correct throughout. One new spin-off: `py_vapid` is imported directly in `mint_vapid_keypair.py` but is only a transitive dependency of `pywebpush`, not declared explicitly in `pyproject.toml` (SNOW-321). `PushSubscription` (added by SNOW-319) joins `Subscription` and `PasskeyCredential` in the BaseModel-exception watching group — it follows the same pattern and is otherwise fully compliant. The three open coverage-gap tickets (SNOW-281, SNOW-282, SNOW-283) carry forward unchanged. SNOW-214 (`_parse_date` consolidation) is confirmed resolved — no `_parse_date` pattern found anywhere in the codebase; watch closed.

## Inline-fixed

| Item | File(s) | Why trivial |
|------|---------|-------------|
| (none) | — | — |

## Spun off

| Item | Child ticket | Why non-trivial |
|------|--------------|-----------------|
| `py_vapid` imported directly in `mint_vapid_keypair.py` but not declared as a direct dep in `pyproject.toml`; arrives only as a transitive dep of `pywebpush` | [SNOW-321](https://linear.app/hugorodgerbrown/issue/SNOW-321) | Multi-file: `pyproject.toml`, `poetry.lock`, `tox.ini`; requires a `tox` pass to verify lock update |

## Watching

| Item | Notes |
|------|-------|
| `public/views.py` coverage gap | Tracked by [SNOW-281](https://linear.app/hugorodgerbrown/issue/SNOW-281). File grew to ~4744 lines since last cycle; 82% at last measure. Carry |
| `public/templatetags/og_tags.py` coverage gap | Tracked by [SNOW-282](https://linear.app/hugorodgerbrown/issue/SNOW-282). 83% at last measure; uncovered path is the `except (AttributeError, IndexError)` branch in `og_locale`. Carry |
| `subscriptions/management/commands/dev_magic_link.py` 0% coverage | Tracked by [SNOW-283](https://linear.app/hugorodgerbrown/issue/SNOW-283). 0% throughout all cycles; `DEBUG=False` guard makes it a no-op in normal test runs. Carry |
| `Subscription`, `PasskeyCredential`, `PushSubscription` not inheriting `BaseModel` | All three manually inline `id`, `uuid`, `created_at`, `updated_at` — constrained by `AbstractBaseUser`/spike design. `PushSubscription` newly added to this group (SNOW-319). No code comment explains the exception. Carry |
| Test `-> None` annotations | 485 of 3111 test functions (15.6%) lack `-> None`; slight improvement from 16.3% (402/2458) last cycle. SNOW-213 ongoing. Carry |
| `backfill_weather` mandatory date args | `--start`/`--end` are effectively required; acceptable for a backfill command where bare invocation is semantically meaningless. Carry from cycles 4 and 5 |
| `print()` in `docs/research/meteofrance/_parse_samples.py` | Research script; acceptable. No action. Carry |
| `config/settings/base.py` imports `logging` without a module-level logger | Settings file; module-level logger unused by convention. Acceptable. No action |
| SNOW-214 — `_parse_date` consolidation | **Resolved** — no `_parse_date` pattern found anywhere in the codebase. Watch closed |

## Checklist results

1. Module-header docstrings — ✅ no drift found (all non-`__init__` modules comply; SNOW-319 additions `subscriptions/push_service.py`, `subscriptions/push_views.py`, `public/decorators.py` fully compliant)
2. Function/class docstrings — ✅ no drift found (spot-checked 20+ functions across `regions/services/point_match.py`, `subscriptions/push_service.py`, `subscriptions/services/request_context.py`, `subscriptions/logging_utils.py`, `public/decorators.py`, `subscriptions/push_views.py`, `subscriptions/admin.py`; all documented)
3. Logging discipline — ✅ no new drift (no `print()` in non-research source; no `logger.error/warning + exc_info=True` in any `except` block; `logger.debug(..., exc_info=True)` at `regions/services/point_match.py:148` is correct form for debug-level tracebacks — not a violation; research-script `print()` and settings-file-logger omission are carry watching)
4. Type annotations (test side) — 👀 watching (485/3111 test functions still lack `-> None`, 15.6%; slight improvement from 16.3% last cycle; SNOW-213 ongoing)
5. Datetime tz-awareness — ✅ no drift found (no `datetime.now()` or `datetime.utcnow()` in production source; new test code uses `datetime(..., tzinfo=UTC)` throughout)
6. Model rules — 👀 watching (all 10 existing concrete models compliant; `PushSubscription` added to BaseModel-exception group alongside `Subscription`/`PasskeyCredential` — otherwise fully compliant: `Meta.ordering`, `to_string()`, `__str__`, custom `QuerySet`, `PushSubscriptionAdmin`, `PushSubscriptionFactory`, dedicated test module)
7. No business logic in models — ✅ no drift found (spot-checked `bulletins/models.py`, `regions/models.py`, `subscriptions/models.py`; model methods are thin accessors or state-transition helpers)
8. No `post_save` signals — ✅ no drift found (only `post_save` reference is a comment in `bulletins/services/day_rating.py` explicitly stating it is NOT used; no `Signal(` or `@receiver` found)
9. HTMX partial views — ✅ no drift found (all three `partials/` routes carry `@require_htmx`; new push views are JSON API endpoints guarded by `@staff_member_required` + `@require_POST`, not `@require_htmx` — correct; no `request.htmx` access outside `core/decorators.py`)
10. No DB lookups in templates/templatetags — ✅ no drift found (no `.objects.` calls in any templatetag file)
11. Management-command shape — ✅ no new drift (18 commands checked; `backfill_pdf_urls` and `parse_wet_snow_coverage` added since last cycle — both compliant; `backfill_weather` mandatory-date watching item unchanged)
12. Project invariants — ✅ all clean (`mark_safe` only on codebase-controlled/sanitised content; email lowercase enforced at `SubscriberQuerySet.get_or_create_for_email` and view entry points; subscription emails and push notifications both async via `@task` + `.enqueue()`; HTMX guards hold; no secrets in source)
13. Dead code — ✅ no drift found (ruff reports all checks passed; no unused imports, no commented-out code blocks in new commits; SNOW-312 rollback cleaned up `subscriptions/fields.py` without leaving orphans)
14. Unused dependencies — 📋 spun off (`py_vapid` imported directly in `mint_vapid_keypair.py` but not declared as a direct dep; [SNOW-321](https://linear.app/hugorodgerbrown/issue/SNOW-321))
15. Pattern consistency — ✅ no new drift (SNOW-214 `_parse_date` consolidation confirmed resolved; no new duplication of helper patterns in SNOW-278/SNOW-319 additions)
16. Test coverage — 👀 watching (tox not run in this environment; carrying 93% overall baseline from 2026-06-03; per-module gaps SNOW-281 `public/views.py` 82%, SNOW-282 `public/templatetags/og_tags.py` 83%, SNOW-283 `dev_magic_link.py` 0% all carry unchanged)
17. Stale TODO/FIXME/XXX/HACK — ✅ no drift found (no `TODO`, `FIXME`, `HACK` markers in any `.py`, `.html`, or `.css` file; `XXXX` strings in `test_slf_fetcher.py` and `dump_resorts_fixture.py` are domain strings, not markers)
