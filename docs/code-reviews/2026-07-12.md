# Code review — 2026-07-12

**Reviewer:** Code review Routine
**Branch:** chore/SNOW-362-code-review-2026-07-12
**Tox baseline:** all green (3659 passed, 7 skipped, 93% coverage)
**Previous cycle:** [2026-06-28](2026-06-28.md) (SNOW-348)

## Summary

Cycle 9 covers commits since 2026-06-28: the diff (`a36b327..HEAD`) touches only `uv.lock`, `.pre-commit-config.yaml`, and `.claude/scheduled_tasks.lock` — SNOW-347's routine dependency bump. Zero application code changed this cycle, so every carry-forward item from cycle 8 is confirmed still open and unchanged. The notable finding is a coverage-visibility gap in the audit process itself: a fresh, full per-module coverage table surfaces several below-90% modules that were never called out in recent cycles' watch lists. Most significant is `bulletins/management/commands/recompute_day_ratings.py`, flat at **0%** coverage — first flagged as SNOW-166 back on 2026-05-13, marked Done the same day without the test module ever landing, and then silently dropped from tracking. That ticket has been reopened this cycle rather than duplicated. Four newly-surfaced, previously-untracked coverage gaps are spun off: `bulletins/services/geoip.py` (74%), `bulletins/services/meteofrance_fetcher.py` (88%), `bulletins/services/meteofrance_translator.py` (87%), and `bulletins/management/commands/backfill_bulletin_groupings.py` (89%). All other carry items (SNOW-349, SNOW-350, SNOW-321, SNOW-336, SNOW-337, `_get_properties` duplication, the `BaseModel`-exception trio) are unchanged from cycle 8. Overall codebase health remains good — this was a quiet, dependency-bump-only cycle with an audit-hygiene correction as its main contribution.

## Inline-fixed

| Item | File(s) | Why trivial |
|------|---------|-------------|
| _(none this cycle — no application code changed)_ | | |

## Spun off

| Item | Child ticket | Why non-trivial |
|------|--------------|-----------------|
| `recompute_day_ratings.py` 0% coverage — closed in 2026-05-13 as Done without the test module landing; reopened rather than duplicated | [SNOW-166](https://linear.app/hugorodgerbrown/issue/SNOW-166) (reopened) | Requires new `tests/bulletins/management/commands/test_recompute_day_ratings.py` covering read-only default, `--commit` path, non-zero exit on partial failure, `--verbosity` |
| `bulletins/services/geoip.py` 74% coverage (61 stmts, 16 missing) — newly tracked | [SNOW-363](https://linear.app/hugorodgerbrown/issue/SNOW-363) | Requires new test cases against a service module; multi-scenario coverage work |
| `bulletins/services/meteofrance_fetcher.py` 88% coverage (181 stmts, 21 missing) — newly tracked | [SNOW-364](https://linear.app/hugorodgerbrown/issue/SNOW-364) | Requires new test cases against a fetcher module; multi-scenario coverage work |
| `bulletins/services/meteofrance_translator.py` 87% coverage (302 stmts, 38 missing) — newly tracked, largest below-90% module bar `public/views.py` | [SNOW-365](https://linear.app/hugorodgerbrown/issue/SNOW-365) | Requires new test cases covering several conditional mapping branches |
| `bulletins/management/commands/backfill_bulletin_groupings.py` 89% coverage (55 stmts, 6 missing) — newly tracked | [SNOW-366](https://linear.app/hugorodgerbrown/issue/SNOW-366) | Requires new test cases against edge-case branches |

## Watching

| Item | Notes |
|------|-------|
| `seed_dev_users.py` 73% coverage | Unchanged from cycle 8. Tracked by SNOW-349 (open, Backlog) |
| `PipelineRun`/`Bulletin`/`RegionBulletin` missing `to_string()` delegator | Unchanged from cycle 8. Tracked by SNOW-350 (open, Backlog) |
| `public/views.py` coverage gap | 84% (1134 stmts, 182 missing) — unchanged from cycle 8. Carry |
| `public/templatetags/og_tags.py` coverage gap | 83% (18 stmts, 3 missing); uncovered path is the `except (AttributeError, IndexError)` branch in `og_locale`. Carry |
| `Subscription`, `PasskeyCredential`, `PushSubscription` not inheriting `BaseModel` | Constrained by `AbstractBaseUser`/spike design. No code comment explains the exception. Carry |
| SNOW-321 — `py_vapid` direct dependency | Still imported at `mint_vapid_keypair.py:46` but absent from `pyproject.toml` runtime deps (present in `tox.ini` deps). Open. Carry |
| SNOW-336 — `observations` app absent from coverage config | Still not in `--cov=` flags in `tox.ini` or `pyproject.toml`; overall 93% figure understated. Open. Carry |
| SNOW-337 — `core/locale.py` 78% | 78% (18 stmts, 4 missing); `except Exception` branch in `primary_language` not exercised. Open. Carry |
| `core/apps.py` 86%, `subscriptions/admin.py` 87%, `subscriptions/backends.py` 88%, `mint_vapid_keypair.py` 86%, `dump_resorts_fixture.py` 89% | All below 90% in narrow error-handling branches; gaps stable. Carry |
| `_get_properties` pattern duplication | Duplicated at `public/views.py:231`, `bulletins/admin.py:273`, `bulletins/management/commands/parse_wet_snow_coverage.py:296`. Below two-caller extraction threshold. Carry |
| `print()` in `docs/research/meteofrance/_parse_samples.py` | Research script; acceptable. Carry |
| `config/settings/base.py` imports `logging` without a module-level logger | Settings convention; acceptable. Carry |
| `regions/services/point_match.py:149` `logger.debug(..., exc_info=True)` | Confirmed correct form (debug-level exception capture), not a violation. Carry |
| Coverage-report stability under `pytest-xdist -n auto` | A cold-start `tox -e test` run this cycle produced an implausible coverage table (several modules falsely at 0%, overall 78% instead of 93%) that resolved correctly on every subsequent re-run. Not reproducible on demand; worth a look if CI ever shows a similar swing. Methodology note, not a code finding. |

## Checklist results

1. Module-header docstrings — ✅ no drift found (zero application files changed since cycle 8)
2. Function/class docstrings — ✅ no drift found (spot-check of 25 functions/classes across `core`, `regions`, `bulletins`, `subscriptions`, `public`, `observations`, `analytics` — all documented)
3. Logging discipline — ✅ no drift found (no `print()` outside `docs/research/meteofrance/_parse_samples.py`; `regions/services/point_match.py:149` `exc_info=True` confirmed correct debug-level form)
4. Type annotations (test side) — ✅ no drift found (no test files changed since cycle 8; SNOW-213 watch remains closed)
5. Datetime tz-awareness — ✅ no drift found (all production `datetime(...)` calls carry explicit `tzinfo=UTC` or `zoneinfo.ZoneInfo(...)`; no naive `datetime.now()`/`datetime.utcnow()` in source)
6. Model rules — 📋 spun off carried (`PipelineRun`/`Bulletin`/`RegionBulletin` missing `to_string()` — SNOW-350 unresolved, unchanged); `Subscription`/`PasskeyCredential`/`PushSubscription` `BaseModel`-exception watch carries; all 19 concrete models confirmed to have factories and test modules
7. No business logic in models — ✅ no drift found
8. No `post_save` signals — ✅ no drift found (only hit is the explanatory "does NOT use post_save" comment at `bulletins/services/day_rating.py:74`)
9. HTMX partial views — ✅ no drift found (all `partials/` routes across `public`, `observations` carry `@require_htmx`; non-`partials/`-prefixed HTMX action endpoints in `subscriptions` correctly decorated per that app's established convention)
10. No DB lookups in templates/templatetags — ✅ no drift found (zero `.objects.` matches across all `templatetags/*.py`)
11. Management-command shape — ✅ no drift found (command set unchanged since cycle 8)
12. Project invariants — ✅ all clean (`mark_safe` sites all carry sanitisation justification; email lowercasing and async subscription-email dispatch unchanged; all partial routes `@require_htmx`; no secrets in source)
13. Dead code — ✅ no drift found (`tox -e lint` clean; `ds-lint` reports 0 template/CSS violations; no new commented-out code blocks)
14. Unused dependencies — 👀 watching (SNOW-321 carry: `py_vapid` still missing from `pyproject.toml` runtime deps; all other runtime deps confirmed used)
15. Pattern consistency — 👀 watching (`_get_properties` duplication unchanged at three sites)
16. Test coverage — 📋 spun off (3659 passed, 7 skipped, 93% overall, reproducibly confirmed. New-to-tracking gaps: `recompute_day_ratings.py` 0% [SNOW-166, reopened], `geoip.py` 74% [SNOW-363], `meteofrance_fetcher.py` 88% [SNOW-364], `meteofrance_translator.py` 87% [SNOW-365], `backfill_bulletin_groupings.py` 89% [SNOW-366]. Carries: `seed_dev_users.py` 73% [SNOW-349], `core/locale.py` 78% [SNOW-337], `public/views.py` 84%, `og_tags.py` 83%, several 86-89% modules stable. `observations` app still absent from coverage config [SNOW-336])
17. Stale TODO/FIXME/XXX/HACK — ✅ no drift found (all raw regex hits are literal-text false positives — `CH-XXXX` domain placeholders, a `Vent XXXX m` regex-comment placeholder, and a `\uXXXX` escape-sequence docstring example — none are stale markers)
