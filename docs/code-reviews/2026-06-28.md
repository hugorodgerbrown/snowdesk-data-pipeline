# Code review — 2026-06-28

**Reviewer:** Code review Routine
**Branch:** chore/SNOW-348-code-review-2026-06-28
**Tox baseline:** all green (3659 passed, 7 skipped, 93% coverage)
**Previous cycle:** [2026-06-21](2026-06-21.md) (SNOW-338)

## Summary

Cycle 8 covers commits since 2026-06-21: SNOW-338 (analytics middleware CDN-caching exemption), SNOW-346 (dev_magic_link test coverage), SNOW-345 (worktree DB seeding from fixtures). Codebase health is good. One positive resolution: `subscriptions/management/commands/dev_magic_link.py` moved from 0% to 100% coverage — SNOW-346 delivered. One inline fix: `dev_magic_link.py` was missing the required module-level `logger = logging.getLogger(__name__)` declaration despite being a production module. Two new spin-offs: `seed_dev_users.py` has fallen to 73% coverage across three uncovered branches (SNOW-349), and `PipelineRun`, `Bulletin`, and `RegionBulletin` each implement `__str__` directly without the required `to_string()` delegator (SNOW-350) — a model-kit gap that previous cycles missed. The e2e `-> None` annotation watch item is fully resolved (all 15 e2e test functions confirmed annotated) — SNOW-213 can be closed. All other carry items are unchanged.

## Inline-fixed

| Item | File(s) | Why trivial |
|------|---------|-------------|
| Missing `logger = logging.getLogger(__name__)` at module level | `subscriptions/management/commands/dev_magic_link.py` | Single-file, one-liner addition, no behaviour change, no new tests needed |

## Spun off

| Item | Child ticket | Why non-trivial |
|------|--------------|-----------------|
| `seed_dev_users.py` at 73% (49 stmts, 13 missing); three uncovered branches: `MicroRegion.DoesNotExist` handler, pending-subscriber reactivation, verbosity-1 output | [SNOW-349](https://linear.app/hugorodgerbrown/issue/SNOW-349) | Requires new test cases in `tests/subscriptions/management/commands/test_seed_dev_users.py`; multi-scenario coverage work |
| `PipelineRun`, `Bulletin`, `RegionBulletin` each implement `__str__` directly without the required `to_string()` delegator mandated by CLAUDE.md model-kit rules | [SNOW-350](https://linear.app/hugorodgerbrown/issue/SNOW-350) | Touches three model definitions in `bulletins/models.py`; admin display tests may need verification after the rename |

## Watching

| Item | Notes |
|------|-------|
| `public/views.py` coverage gap | 84% (1134 stmts, 182 missing) — file grew by 26 stmts vs last cycle (SNOW-342/343/344 additions) but absolute missing count held at 182. Still below 90% target. Carry |
| `public/templatetags/og_tags.py` coverage gap | 83% (18 stmts, 3 missing); uncovered path is the `except (AttributeError, IndexError)` branch in `og_locale`. Carry |
| `Subscription`, `PasskeyCredential`, `PushSubscription` not inheriting `BaseModel` | All three manually inline `id`, `uuid`, `created_at`, `updated_at` — constrained by `AbstractBaseUser`/spike design. No code comment explains the exception. Carry |
| Test `-> None` annotations | **Resolved.** All 15 e2e test functions carry `-> None` annotations (verified). Non-e2e suite also clean. SNOW-213 watch closed |
| SNOW-321 — `py_vapid` direct dependency | `py_vapid` imported at `mint_vapid_keypair.py:46` but absent from `pyproject.toml` runtime deps (present in `tox.ini` deps). SNOW-321 remains open. Carry |
| SNOW-336 — `observations` app absent from coverage config | `observations/` still not in `--cov=` flags in `tox.ini` or `pyproject.toml`; overall 93% figure understated. SNOW-336 open. Carry |
| SNOW-337 — `core/locale.py` 78% | 78% (18 stmts, 4 missing); `except Exception` branch in `primary_language` not exercised. SNOW-337 open. Carry |
| `core/apps.py` 86%, `subscriptions/admin.py` 87%, `subscriptions/backends.py` 88%, `mint_vapid_keypair.py` 86%, `dump_resorts_fixture.py` 89% | All below 90% in narrow error-handling branches; gaps are stable. Carry |
| `_get_properties` pattern duplication | Duplicated at `public/views.py:231`, `bulletins/admin.py:273`, `bulletins/management/commands/parse_wet_snow_coverage.py:296`. Below two-caller extraction threshold. Carry |
| `print()` in `docs/research/meteofrance/_parse_samples.py` | Research script; acceptable. Carry |
| `config/settings/base.py` imports `logging` without a module-level logger | Settings convention; acceptable. Carry |
| `regions/services/point_match.py:149` `logger.debug(..., exc_info=True)` | Debug-level exception capture requires `exc_info=True` since `logger.exception()` is always ERROR level. Confirmed correct form, not a violation. Carry |

## Checklist results

1. Module-header docstrings — ✅ no drift found (all modules introduced since the previous cycle carry proper module docstrings; `observations/migrations/0005_fieldobservation_user.py` uses the standard migration comment convention)
2. Function/class docstrings — ✅ no drift found (spot-checked 20 functions/classes across new code: `dev_magic_link.Command.handle/add_arguments`, `seed_dev_users.Command.handle`, `analytics.__init__._assert_no_pii/track`, `bulletins.services.coverage.covered_region_ids`, `observations.views.report_form/report_submit`, `public.views.fetch_weather_snippet/season_calendar_partial`, `subscriptions.models.SubscriberQuerySet.get_or_create_for_email`, `subscriptions.models.PasskeyCredential.display_name/to_string`, `subscriptions.models.PushSubscription.to_string`, `core.middleware.QueryCountMiddleware.__call__`, `core.middleware.SecurityHeadersMiddleware.__call__`, plus `regions.services.point_match` and `bulletins.services.grouping` helpers — all documented)
3. Logging discipline — 🔧 inline-fixed (`dev_magic_link.py` was missing `logger = logging.getLogger(__name__)` — added. All other new production modules compliant. Research-script `print()` and settings-file logger absence carry as watching)
4. Type annotations (test side) — ✅ **resolved** (all 15 e2e test functions at `tests/e2e/` carry `-> None` annotations; non-e2e suite also at 0 missing. SNOW-213 watch closed)
5. Datetime tz-awareness — ✅ no drift found (no `datetime.now()` or `datetime.utcnow()` calls in production source; all `datetime(...)` constructor calls in test files carry explicit `tzinfo=UTC`)
6. Model rules — 📋 spun off (`PipelineRun`, `Bulletin`, `RegionBulletin` each implement `__str__` directly without the required `to_string()` delegator → SNOW-350. New concrete models: none this cycle. `Subscription`/`PasskeyCredential`/`PushSubscription` carry in the BaseModel-exception watching group)
7. No business logic in models — ✅ no drift found (model methods across `bulletins/models.py`, `subscriptions/models.py`, `observations/models.py` are thin accessors, QuerySet factory methods, or `Counter` aggregations — no I/O or network calls)
8. No `post_save` signals — ✅ no drift found (only `post_save` reference is the explanatory comment in `bulletins/services/day_rating.py:74` explicitly stating it is NOT used; no `Signal(` or `@receiver` found)
9. HTMX partial views — ✅ no drift found (all five `partials/` routes verified: `public:weather_snippet`, `public:season_partial`, `public:components_panel` in `debug_views.py`, `observations:report_form`, `observations:report_submit` — all carry `@require_htmx`; `ds-lint` passes with 0 violations)
10. No DB lookups in templates/templatetags — ✅ no drift found (grep of all `**/templatetags/*.py` for `.objects.` returns zero matches)
11. Management-command shape — ✅ no drift found (all commands enumerated; new commands follow the `--commit`/read-only-by-default shape; `fetch_bulletins --source` required flag is an operational necessity documented in `docs/management-commands.md`; `dev_magic_link` and `seed_dev_users` are dev-only DEBUG-gated commands — acceptable)
12. Project invariants — ✅ all clean (`mark_safe` only on empty strings or `bleach`-sanitised content; email lowercase enforced at `SubscriberQuerySet.get_or_create_for_email` and form `clean()` methods; subscription emails async via `@task()` + `.enqueue()`; all `partials/` endpoints carry `@require_htmx`; no credentials in source)
13. Dead code — ✅ no drift found (`tox -e lint` ruff passes clean; no unused imports in new commits; no commented-out code blocks in modified files)
14. Unused dependencies — 👀 watching (SNOW-321 carry: `py_vapid` directly imported in production code but absent from `pyproject.toml` runtime deps; all other runtime deps have confirmed import or CLI usage)
15. Pattern consistency — 👀 watching (`_get_properties` duplication unchanged at three sites; no new pattern inconsistencies introduced this cycle)
16. Test coverage — 📋 spun off (3659 passed, 7 skipped, 93% overall. Resolved: `dev_magic_link.py` 0% → 100% (SNOW-346). New spin-off: `seed_dev_users.py` 73% → SNOW-349. Carry: `observations` absent from coverage config (SNOW-336); `core/locale.py` 78% (SNOW-337); `public/views.py` 84%; `og_tags.py` 83%; several 86–89% modules stable)
17. Stale TODO/FIXME/XXX/HACK — ✅ no drift found (no `TODO`, `FIXME`, or `HACK` markers in any `.py`, `.html`, or `.css` file; `XXXX` strings in test files and scripts are domain strings, not stale markers)
