# Code review — 2026-07-05

**Reviewer:** Code review Routine
**Branch:** claude/charming-lovelace-tzl6or
**Tox baseline:** all green (3659 passed, 7 skipped, 93% coverage)
**Previous cycle:** [2026-06-28](2026-06-28.md) (SNOW-348)

## Summary

Cycle 9 covers exactly one commit since the 2026-06-28 cycle — `9e679e7`
(SNOW-347), a pure dependency-lock advance (anyio, apscheduler, coverage,
django-stubs, djangofmt, posthog, ruff, semgrep, tox) with the matching
`.pre-commit-config.yaml` ruff rev bump. No application source changed, so
the test/coverage numbers are bit-for-bit identical to cycle 8. `tox -e
lint`, `mypy`, `django-checks`, `ds-lint`, and `docs-lint` are all clean,
confirming the dependency bump introduced no fallout. A ticket-hygiene gap
surfaced this cycle: **SNOW-349** and **SNOW-350**, both surfaced by cycle 8
and marked **Done** on 2026-06-29, were never actually fixed — neither
`subscriptions/management/commands/seed_dev_users.py` nor
`bulletins/models.py` has been touched since before those tickets closed.
Both have been reopened (moved back to Backlog) with an explanatory
comment rather than spawning duplicate tickets. One genuinely new finding:
`bulletins/management/commands/recompute_day_ratings.py` has sat at 0%
coverage (67/67 statements) since it landed via the SNOW-312 PR in
mid-June — missed by every cycle from 06-15 through 06-28 — spun off as
SNOW-360.

## Inline-fixed

None this cycle — no application source changed since the previous cycle.

## Spun off

| Item | Child ticket | Why non-trivial |
|------|--------------|-----------------|
| `bulletins/management/commands/recompute_day_ratings.py` at 0% coverage (67 stmts, 0 covered); no test module exists | [SNOW-360](https://linear.app/hugorodgerbrown/issue/SNOW-360/add-test-coverage-for-recompute-day-ratings-management-command) | Requires a new test module covering `add_arguments`, `handle` (default + `--commit`), `_collect_pairs`, and `_process_pairs` including the `MicroRegion.DoesNotExist` and `failed > 0` branches |
| `PipelineRun`, `Bulletin`, `RegionBulletin` each implement `__str__` directly without the required `to_string()` delegator | [SNOW-350](https://linear.app/hugorodgerbrown/issue/SNOW-350/add-to-string-delegator-to-pipelinerun-bulletin-and-regionbulletin) (reopened — closed 2026-06-29 without the fix landing) | Touches three model definitions in `bulletins/models.py`; admin display tests may need verification after the rename |
| `seed_dev_users.py` at 73% (49 stmts, 13 missing); three uncovered branches unchanged since cycle 8 | [SNOW-349](https://linear.app/hugorodgerbrown/issue/SNOW-349/raise-seed-dev-userspy-test-coverage-from-73percent-to-90percent) (reopened — closed 2026-06-29 without the fix landing) | Requires new test cases in `tests/subscriptions/management/commands/test_seed_dev_users.py`; multi-scenario coverage work |

## Watching

| Item | Notes |
|------|-------|
| `observations` app absent from coverage config | `tox.ini` `--cov=` flags and `pyproject.toml` `[tool.coverage.run].source` still omit `observations/`; overall 93% figure understated. SNOW-336 open. Carry |
| `py_vapid` direct dependency | Still imported at `mint_vapid_keypair.py:46` but absent from `pyproject.toml` runtime deps (present in `tox.ini` deps). SNOW-321 open. Carry |
| `core/locale.py` 78% | 78% (18 stmts, 4 missing); `except Exception` branch in `primary_language` unexercised. SNOW-337 open. Carry |
| `public/views.py` coverage gap | 84% (1134 stmts, 182 missing), stable — no growth since cycle 8. Carry |
| `public/templatetags/og_tags.py` coverage gap | 83% (18 stmts, 3 missing); `except (AttributeError, IndexError)` branch in `og_locale` unexercised. Carry |
| `bulletins/services/geoip.py` 74% | Re-verified this cycle: gap is entirely inside `@requires_geoip_db`-skipped paths (fires when the MaxMind `GeoLite2-City.mmdb` file isn't present). Intentional, environment-gated, stable. No action |
| `core/apps.py` 86%, `subscriptions/admin.py` 87%, `subscriptions/backends.py` 88%, `mint_vapid_keypair.py` 86%, `dump_resorts_fixture.py` 89% | All below 90% in narrow error-handling branches; figures identical to cycle 8. Carry |
| `_get_properties` pattern duplication | Duplicated at `public/views.py:231`, `bulletins/admin.py:273`, `bulletins/management/commands/parse_wet_snow_coverage.py:296`. Below two-caller extraction threshold. Carry |
| `Subscription`, `PasskeyCredential`, `PushSubscription` not inheriting `BaseModel` | Constrained by `AbstractBaseUser`/spike design; no code comment explains the exception. Carry |
| `regions/models.py` `MicroRegionNeighbour` through-table | Plain `models.Model` self-referential M2M through-table — no `BaseModel`, `to_string()`, admin registration, or dedicated factory/test module. Unflagged since at least 2026-05-05; treated as an accepted through-table exception. Noting for visibility only |
| `print()` in `docs/research/meteofrance/_parse_samples.py` | Research script; acceptable. Carry |
| `config/settings/base.py` imports `logging` without a module-level logger | Settings convention; acceptable. Carry |
| `regions/services/point_match.py:149` `logger.debug(..., exc_info=True)` | Confirmed correct form (debug-level exception capture requires explicit `exc_info=True`), not a violation. Carry |

## Checklist results

1. Module-header docstrings — ✅ no drift found (every non-migration, non-test `.py` across all eight apps carries a docstring or the project's header-comment convention; bare `__init__.py` files remain content-free, same stable state as every prior cycle)
2. Function/class docstrings — ✅ no drift found (spot-checked 43 functions/classes across `regions/models.py`, `regions/management/commands/dump_resorts_fixture.py`, `bulletins/services/openmeteo_archive.py`, `public/checks.py` — zero missing docstrings)
3. Logging discipline — ✅ no drift found (zero `print()` in non-research production source; `exc_info=True` occurs only at `regions/services/point_match.py:149` in the confirmed-correct debug-level form; `config/settings/base.py` remains the sole accepted exception; all 75 production modules with `import logging` carry `getLogger(__name__)`)
4. Type annotations (test side) — ✅ no drift found (AST-verified: 0 of 3364 `test_*` functions lack a `-> None` return annotation; SNOW-213 remains fully resolved)
5. Datetime tz-awareness — ✅ no drift found (no naive `datetime.now()`/`datetime.utcnow()` in production source; all `datetime(...)` constructor calls carry explicit `tzinfo=UTC` or a `ZoneInfo` instance)
6. Model rules — 📋 spun off (`PipelineRun`, `Bulletin`, `RegionBulletin` still implement `__str__` directly with no `to_string()` delegator — SNOW-350 reopened. `Subscription`/`PasskeyCredential`/`PushSubscription` BaseModel-exception and `MicroRegionNeighbour` through-table exception carry as watching, unchanged)
7. No business logic in models — ✅ no drift found (spot-checked `bulletins/models.py`, `subscriptions/models.py`, `regions/models.py`, `observations/models.py` methods — thin accessors, QuerySet helpers, dataclass conversions only)
8. No `post_save` signals — ✅ no drift found (only match for `post_save`/`Signal(`/`@receiver` across the tree is the explanatory "does NOT use post_save" comment at `bulletins/services/day_rating.py:74`)
9. HTMX partial views — ✅ no drift found (all 5 `partials/`-prefixed routes carry `@require_htmx`; zero `request.htmx` references outside decorated views; `ds-lint` reports 0 violations)
10. No DB lookups in templates/templatetags — ✅ no drift found (`.objects.` grep across all 7 files in `public/templatetags/` returns zero matches)
11. Management-command shape — 📋 spun off (see #16) (all 22 commands enumerated; `recompute_day_ratings.py` itself is contract-compliant — no-arg default is read-only, `--commit` gate, respects `--verbosity`, raises `CommandError` on `failed > 0` — the gap is purely test coverage, not command shape)
12. Project invariants — ✅ all clean (`mark_safe` only on numeric/static/bleach-sanitised content with `# noqa: S308` justification; email lowercased at every entry point; `send_mail` calls only inside `@task()`-decorated workers; all `partials/` endpoints `@require_htmx`-guarded; no credentials in source)
13. Dead code — ✅ no drift found (`tox -e lint` passes clean; no commented-out code blocks found; `ds-lint` finds 0 dead template partials/violations)
14. Unused dependencies — 👀 watching (`py_vapid` still absent from `pyproject.toml` runtime deps despite direct import — SNOW-321 open, unchanged)
15. Pattern consistency — 👀 watching (`_get_properties` duplication unchanged at the same three sites; no new pattern inconsistencies, as expected given zero source changes since last cycle)
16. Test coverage — 📋 spun off (3659 passed, 7 skipped, 93% overall — identical to cycle 8. New: `recompute_day_ratings.py` at 0% → SNOW-360. Reopened: `seed_dev_users.py` 73% → SNOW-349; `observations` absent from coverage config remains SNOW-336; `core/locale.py` 78% remains SNOW-337; `public/views.py` 84%, `og_tags.py` 83%, `geoip.py` 74% (confirmed intentional), and the 86–89% cluster all carry)
17. Stale TODO/FIXME/XXX/HACK — ✅ no drift found (zero markers in any `.py`/`.html`/`.css` file; the `XXXX`/`XXX` string matches in `scripts/meteofrance-archive/mf_bra_to_caaml.py:535`, `tests/bulletins/services/test_slf_fetcher.py:339/354`, and `regions/management/commands/dump_resorts_fixture.py:114` are domain placeholder/format strings, not stale markers)
