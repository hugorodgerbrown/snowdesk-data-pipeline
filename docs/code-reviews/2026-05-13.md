# Code review — 2026-05-13

**Reviewer:** Hugo Rodger-Brown
**Branch:** feature/SNOW-165
**Tox baseline:** all green (1442 passed, 1 skipped, 93% coverage)
**Previous cycle:** [2026-05-05](2026-05-05.md) (SNOW-112)

## Summary

Codebase remains in good health following the large restructuring sprint (app
split, Django 6.0 upgrade, custom user model, passkeys, weather header). Across
all 17 audit items only one trivial configuration drift item was found and
fixed inline. Three test-coverage gaps were spun off as child tickets: the
newly-added `recompute_day_ratings` command has no test module, `public/views.py`
has dropped to 83% coverage following several new view functions in SNOW-159 /
SNOW-164, and four tests in `test_pwa_apple_meta.py` have an order-dependent
isolation bug (missing `@pytest.mark.django_db`). Overall coverage is 93%, no
invariant violations, no HTMX-guard drift, no stale TODOs, and no dead code.

## Inline-fixed

| Item | File(s) | Why trivial |
|------|---------|-------------|
| Stale `--cov=pipeline` in `addopts` and `coverage.run.source`; missing `--cov=regions` in `addopts` | `pyproject.toml` (lines 91, 101–102) | Config-only edit; no behaviour change when running via tox (tox.ini was already correct) |

## Spun off

| Item | Child ticket | Why non-trivial |
|------|--------------|-----------------|
| `recompute_day_ratings` command — 0% coverage | [SNOW-166](https://linear.app/hugorodgerbrown/issue/SNOW-166) | Needs new test module with command-invocation fixtures |
| `public/views.py` — 83% coverage (below 90% threshold) | [SNOW-167](https://linear.app/hugorodgerbrown/issue/SNOW-167) | Many uncovered branches across multiple view functions; needs significant new tests |
| `test_pwa_apple_meta.py` — missing `@pytest.mark.django_db`; tests pass only because other tests run first | [SNOW-168](https://linear.app/hugorodgerbrown/issue/SNOW-168) | Behaviour change to test isolation; needs careful review of test ordering assumptions |

## Watching

| Item | Notes |
|------|-------|
| `RegionBulletin` admin coverage | Carry from SNOW-112. `RegionBulletinInline` under `BulletinAdmin`, no top-level admin. CODING_STANDARDS says "every model has an explicit AdminModel" — inline arguably satisfies. Deferred again; re-check next cycle |
| `Subscription` / `PasskeyCredential` not inheriting `BaseModel` | Fields (`uuid`, `created_at`, `updated_at`) are inlined rather than inherited. Subscriber can't use `BaseModel` due to `AbstractBaseUser`; the related models follow the same pattern for co-location. Pattern inconsistency but intentional. No comment on the related models explaining the choice |
| `subscriptions/management/commands/dev_magic_link.py` — 0% coverage | Dev-only tool; excluded from the spin-off because it's deliberately out of scope for normal test runs |

## Checklist results

1. Module-header docstrings — ✅ no drift found (all non-`__init__` modules have headers)
2. Function/class docstrings — ✅ no drift found (spot-check of 20+ functions across render_model, views, services)
3. Logging discipline — ✅ no drift found (no `print()`; `logger.error(..., exc_info=True)` used where appropriate; `logger.exception` where no explicit `exc_info` needed)
4. Type annotations (test side) — ✅ no drift found (mypy override for tests is intentional; production code fully annotated)
5. Datetime tz-awareness — ✅ no drift found (no `datetime.utcnow()` or naive `datetime()` calls)
6. Model rules — ✅ no new drift (existing `RegionBulletin` / `Subscription` / `PasskeyCredential` exceptions unchanged from last cycle; all new models comply)
7. No business logic in models — ✅ no drift found (`PipelineRun.mark_*` methods are lifecycle state transitions, not domain logic)
8. No `post_save` signals — ✅ no drift found
9. HTMX partial views — ✅ no drift found (`fetch_weather_snippet` at `partials/weather/` is `@require_htmx`-decorated; all other partial routes verified)
10. No DB lookups in templates/templatetags — ✅ no drift found
11. Management-command shape — ✅ no drift found (`recompute_day_ratings` respects `--commit`, `--verbosity`, exits non-zero on failure)
12. Project invariants — ✅ no drift found (all five hold: `mark_safe` on sanitised output only; email normalised; Resend async via threading; `@require_htmx` on all partials; no secrets in source)
13. Dead code — ✅ no drift found
14. Unused dependencies — ✅ no drift found (`shapely` used by `refresh_eaws_fixtures`; `semgrep` is the SAST runner; `pre-commit` is dev tooling)
15. Pattern consistency — 👀 watching (`Subscription`/`PasskeyCredential` inline `BaseModel` fields without inheriting `BaseModel` — intentional but uncommented on those models)
16. Test coverage — 📋 spun off ([SNOW-166](https://linear.app/hugorodgerbrown/issue/SNOW-166), [SNOW-167](https://linear.app/hugorodgerbrown/issue/SNOW-167), [SNOW-168](https://linear.app/hugorodgerbrown/issue/SNOW-168))
17. Stale TODO/FIXME/XXX/HACK — ✅ no drift found
