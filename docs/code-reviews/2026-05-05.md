# Code review — 2026-05-05

**Reviewer:** Hugo Rodger-Brown
**Branch:** chore/SNOW-112-code-review-2026-05-05
**Tox baseline:** all green (1179 passed, 1 skipped, 94% coverage)
**Previous cycle:** none (first cycle — bootstrapped via SNOW-111)

## Summary

Codebase is in good health. Across all 17 audit items only two trivial drift items were found and fixed inline. Two test-coverage gaps below the 90% target were spun off as child tickets. Overall coverage is 94%, no invariant violations, no HTMX-guard drift, no stale TODOs, and dependencies are all justified.

## Inline-fixed

| Item | File(s) | Why trivial |
|------|---------|-------------|
| Comment-before-docstring + dead `# @lru_cache` + misleading docstring claim | `public/guidance.py` (lines 1, 9, and `load_field_guidance` docstring) | Comment + docstring edit only; no behaviour change |
| Stale architecture description (4 bulletin commands listed, actually 6; pipeline commands undocumented) | `CLAUDE.md` Architecture section | Documentation-only edit |
| Bootstrap longitudinal review structure | `docs/code-reviews/README.md` (new), `CLAUDE.md` Feature-reference table (new row) | One-off bootstrapping for the cycle template |

## Spun off

| Item | Child ticket | Why non-trivial |
|------|--------------|-----------------|
| `bulletins/admin.py` coverage 83% → 90% | [SNOW-113](https://linear.app/hugorodgerbrown/issue/SNOW-113) | Needs new tests for admin display methods, readonly_fields callables, and inline rendering |
| `public/templatetags/` coverage 82% → 90% (`card_tags.py` + `snowdesk_time.py`) | [SNOW-114](https://linear.app/hugorodgerbrown/issue/SNOW-114) | Needs new templatetag rendering tests |

## Watching

| Item | Notes |
|------|-------|
| `RegionBulletin` admin coverage | Currently `RegionBulletinInline` under `BulletinAdmin`, no top-level admin (`bulletins/admin.py:68`). `CODING_STANDARDS.md` says "every model has an explicit AdminModel" — an inline arguably satisfies that. Decision deferred; re-check next cycle |
| `public/guidance.py` caching | Restoring `@lru_cache(maxsize=1)` would mean YAML edits require server restart. Behaviour decision; current behaviour preserved this cycle |
| `public/api.py` coverage at 94% | Close to the 90% threshold but well above. Monitor |

## Checklist results

1. Module-header docstrings — 🔧 fixed inline (`public/guidance.py:1`)
2. Function/class docstrings — ✅ no drift found
3. Logging discipline — ✅ no drift found
4. Type annotations (test side) — ✅ no drift found
5. Datetime tz-awareness — ✅ no drift found
6. Model rules — 👀 watching (`RegionBulletin` admin coverage)
7. No business logic in models — ✅ no drift found
8. No `post_save` signals — ✅ no drift found (explicit anti-comment in `bulletins/services/day_rating.py`)
9. HTMX partial views — ✅ no drift found (only two `partials/` routes; both decorated)
10. No DB lookups in templates/templatetags — ✅ no drift found
11. Management-command shape — ✅ no drift found
12. Project invariants — ✅ no drift found (all five hold)
13. Dead code — 🔧 fixed inline (`public/guidance.py:9` commented-out `@lru_cache`)
14. Unused dependencies — ✅ no drift found (`shapely` and `semgrep` are CLI-only, both legitimately referenced)
15. Pattern consistency — ✅ no drift found
16. Test coverage — 📋 spun off ([SNOW-113](https://linear.app/hugorodgerbrown/issue/SNOW-113), [SNOW-114](https://linear.app/hugorodgerbrown/issue/SNOW-114))
17. Stale TODO/FIXME/XXX/HACK — ✅ no drift found
