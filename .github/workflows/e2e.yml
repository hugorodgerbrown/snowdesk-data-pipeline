# .github/workflows/e2e.yml — End-to-end browser tests (Playwright / Chromium).
#
# Runs the ``tox -e e2e`` env on every pull-request and push to main/release.
# Isolated from ci.yml so the Chromium install step and its cache are
# scoped to this workflow and do not bloat the main CI image.
#
# "Playwright smoke tests" is a required status check in the "Main branch
# rules" ruleset, so the ``pull_request`` trigger runs on every PR with no
# path filter — a path-filtered required check never reports on PRs outside
# its paths and leaves them stuck at "Expected" (SNOW-435). The ``push``
# trigger keeps a path filter: pushes to main/release are not merge-gated.
# Docs-only PRs skip the test job via the ``changes`` gate — a job-level
# skip still reports a (skipped) check run that satisfies the ruleset, so
# the SNOW-435 trap does not apply (SNOW-481).
#
# The Playwright binary cache is keyed on uv.lock so a new cache entry
# is created only when the Playwright package version changes.  Cache misses
# fall back to a clean install (the Chromium download takes ~60 s on a
# GitHub-hosted runner).

name: End-to-end (Playwright)

on:
  # The push trigger is deliberately NOT path-filtered — see SNOW-600. Every
  # commit on main is a release candidate, and the "Release branch" ruleset
  # requires this workflow's checks on the commit it fast-forwards to. A
  # `paths:` filter here means the workflow is never *scheduled* on a commit
  # that misses it, so the required check never reports and the release push
  # is blocked forever. That is different from a job-level skip, which still
  # reports a (skipped) check run and satisfies the ruleset. Cost control for
  # PRs belongs in the `changes` gate below, not here.
  push:
    branches:
      - main
      - release

  pull_request:
    types: [opened, synchronize, reopened]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

# Least-privilege default: every job gets a read-only token unless it
# overrides this with a narrower/wider scope (SNOW-506).
permissions:
  contents: read

jobs:
  changes:
    name: Detect skippable diff
    runs-on: ubuntu-latest
    # The docs-only filter lists PR files via the REST API, which needs
    # pull-requests:read on top of the default contents:read.
    permissions:
      contents: read
      pull-requests: read
    outputs:
      # "true" unless this pull request has nothing to test — docs-only, or
      # a release PR whose only diff is VERSION; push runs skip the filter
      # step and fall through to "true".
      code: ${{ steps.filter.outputs.code || 'true' }}
    steps:
      - name: Check the PR diff for non-docs files
        id: filter
        if: github.event_name == 'pull_request'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          # Fail open: an empty file list (API error) runs the full suite.
          files="$(gh api "repos/${{ github.repository }}/pulls/${{ github.event.pull_request.number }}/files" --paginate --jq '.[].filename')" || files=""
          code=false
          [ -n "$files" ] || code=true
          while IFS= read -r f; do
            case "$f" in
              # `VERSION` alone is a release PR (bin/cut-release): the same
              # tree main was tested on, plus the release ordinal. The push
              # run on the merged commit tests it again, and that is the run
              # the "Release branch" ruleset actually reads.
              "" | *.md | docs/* | .claude/* | VERSION) ;;
              *) code=true ;;
            esac
          done <<< "$files"
          echo "skippable-diff filter -> code=$code"
          echo "code=$code" >> "$GITHUB_OUTPUT"

  e2e:
    name: Playwright smoke tests
    runs-on: ubuntu-latest
    needs: changes
    if: needs.changes.outputs.code == 'true'

    steps:
      - name: Check out the repository
        uses: actions/checkout@v7

      - name: Set up Python (3.14)
        uses: actions/setup-python@v7
        with:
          python-version: "3.14"

      - name: Create .env
        run: |
          echo "SECRET_KEY=ci-e2e-secret-key" > .env
          echo "DJANGO_SETTINGS_MODULE=config.settings.development" >> .env

      # The e2e tests drive the real page, so the Tailwind stylesheet
      # (``static/css/output.css``, a gitignored build artefact) must be
      # present before the live server serves statics, or any test that
      # clicks a CSS-positioned control fails its Playwright actionability
      # check. The tox ``e2e`` env builds it in ``commands_pre`` (so a local
      # ``tox -e e2e`` works too); Node + ``npm ci`` are here only so that
      # ``npx @tailwindcss/cli`` inside commands_pre resolves the pinned CLI
      # from node_modules instead of downloading it at run time.
      - name: Set up Node (20)
        uses: actions/setup-node@v7
        with:
          node-version: "20"

      - name: Install Node dependencies
        run: npm ci

      - name: Cache Playwright browsers
        uses: actions/cache@v6
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ hashFiles('uv.lock') }}

      # Cache uv's download/wheel store so the tox e2e env (installed via the
      # uv-venv-lock-runner from uv.lock — ~30 packages including shapely and
      # cryptography) reuses wheels across runs instead of re-downloading them.
      # Keyed on uv.lock so a dependency change busts the cache.
      - name: Cache uv downloads
        uses: actions/cache@v6
        with:
          path: ~/.cache/uv
          key: uv-e2e-${{ hashFiles('uv.lock') }}
          restore-keys: |
            uv-e2e-

      - name: Install tox and run e2e env
        run: |
          pip install tox tox-uv
          tox -e e2e
