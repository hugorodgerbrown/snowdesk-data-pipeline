"""
regions/services — Service layer for the regions application.

Provides pure-Python helpers used at request time (e.g. point-in-polygon
classification) that deliberately avoid runtime dependencies on heavy
geo libraries.
"""
